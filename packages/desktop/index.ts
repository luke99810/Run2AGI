/**
 * @codentum/desktop —— 桌面控制台（Electron）
 *
 * ★★ 对系统状态零写权限 —— 纯读端。订阅状态，做投影。
 *
 *   所有写入走控制平面的正式动作，不从 UI 抄近路。
 *   从 UI 直接改状态文件会绕过准入校验和审计链 —— 六条不变量全部失效。
 *
 * ★ Console 挂掉，系统照常跑。
 *   这既是架构属性，也是这个 package 的 owner 犯错不会传播的原因 ——
 *   C 拿桌面端不是因为它简单，是因为它爆炸半径最小。
 *
 * ★ 不依赖 control-plane 的实现，只依赖 contracts 的类型 + fixtures/golden-state 的数据。
 *   所以 C 第一天就能开工，不用等 A 把调度跑起来。
 *   这是「前端依赖 mock 不依赖实现」的同一招 —— 前后端并联的原则同样适用于人。
 *   没有这个解耦点，C 会空等两周然后在最后一周赶工，这是三人团队最常见的死法。
 *
 * ★ 不重新实现判定逻辑。门禁怎么判是 control-plane/gates 的事，UI 只渲染结果。
 * ★ 成本显示用货币，不用 token（分词器差异 ~30%，异构路由下 token 不可比）。
 *
 * owner: C ｜ 评审: B ｜ 详见 README.md
 *
 * ── 待实现（各子目录）────────────────────────────────────
 *   shell/   Electron 主进程 · 窗口 · IPC
 *   views/   甘特图 · 依赖图 · 证据/审计 · 成本
 *   panels/  Skills · MCP · 连接器 · 审批
 *   inputs/  需求录入 · 审批 · Provisioning 表单
 *   mock/    ★ 基于 golden-state 的假数据源，与真数据源同接口
 *
 * ── 第一件该做的事 ──────────────────────────────────────
 *   拿 fixtures/golden-state/ 的三个快照渲染出看板。
 *   完成定义（机器可判定）：三个快照渲染结果与预期截图一致，
 *   且 empty 快照不崩 —— ★「没有数据」是所有渲染代码的第一个边界。
 */

export {};
