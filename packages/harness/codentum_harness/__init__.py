"""codentum_harness —— 执行外壳

把一个 WorkPacket 变成一次受控的模型执行。
【单次执行】的可靠性归这里；多次执行的收敛归控制平面。

★★ 三段式，只有中段有模型：

    准备阶段          →      模型阶段        →      收敛阶段
    【无模型】                【有模型】             【无模型】
    装上下文 · 派生工具面      ★ LangGraph 图        解析产出 · 写证据
    挂载卷(ro/rw) · 申请锁                          判定形式合规

  进模型前，约束已经物理生效；出模型后，判定由代码做出。

★ 约束优先级：不可见 > 无权限 > 被拦截 > 提示词劝阻
  在提示词里写"请不要修改测试文件"是最弱的一档。
  正确做法是把 tests/ 以只读方式挂载 —— 它在物理上就改不了。

★ LangGraph 只用在模型阶段（ADR-0004），且必须守住边界：
    图的 state 是【一次执行的临时状态】，永远不会成为系统状态。
    节点【不得直接写 .codentum/】—— 那会绕过准入校验与审计链。
    出口只有一个：产出 WorkerOutcome（含 evidence 引用）。
  判据：把 LangGraph 整个换掉，控制平面应当一行都不用改。

★ 不 import codentum_control_plane。
★ 同一次尝试内不切换模型 —— 缓存按模型分桶，换模型 = 整个前缀冷重写。
  要加强就提高 effort。

owner: B ｜ 评审: A ｜ 详见 ../README.md
"""
