# Run2AGI / Codentum 项目进展与记忆

> 最后更新：2026-08-09
> 每次涉及本项目的任务都更新此文件；Agent 启动时读取此文件恢复上下文

---

## ⚠️ 这份文件的定位：**派生视图，不是真源**

**本文仅供团队协作与进度同步参考。具体改动一律以事实文件为准。**

它是一份**方便人读的摘要**，用来快速恢复上下文、看清现在做到哪儿了。
它会滞后、会遗漏、也可能写错。**当它与事实文件冲突时，事实文件对，本文错。**

### 事实文件分别是哪些

| 你想知道的事 | 唯一真源 | 别看这里 |
|---|---|---|
| 代码实际怎么做的 | **源码与测试** | 本文的描述 |
| 数据长什么形状 | `packages/contracts/schemas/*.json` | 本文，或任何 md |
| 接口签名 | `packages/contracts/python/…/interfaces.py`（手写，非生成物） | 本文 |
| 谁能写哪些路径 | **`boundaries.yaml`** | 本文 §八 的目录树 |
| 角色权限 / 状态转换表 / 工具面 | **RoleSpec**（`packages/roles/**`） | 任何手写的表 |
| 某个决策为什么这么定 | **`docs/adr/`** | 本文的一句话摘要 |
| 到底发生过什么、谁改的 | **`git log`** | 本文的「最近任务」表 |

> ★ 这不是一句免责声明，是把本项目的核心主张用在自己身上：
> **同一件事有两个地方可查，它们迟早会不一致，而不一致时通常不报错。**
> 所以每件事都必须指定唯一真源，其余一律是派生视图 —— 本文就是派生视图。

### 发现不一致时怎么办

**改本文去对齐事实文件，不要改事实文件去迁就本文。**

如果确实是事实文件错了，那就改事实文件 —— 但那是一次真正的变更，
要走它自己的流程（契约走变更窗口、代码过测试与门禁），
而不是先在这里改一笔，让两边看起来一致了事。

---

## 一、项目概况

| 项 | 内容 |
|----|------|
| 项目名 | **Codentum**（Code + Momentum） |
| 一句话 | 自进化软件开发多 Agent 系统；一句话需求 → 可交付应用 |
| 产品形态 | **本地优先的桌面软件**，对标 OpenClaw / OpenWorker / WorkBuddy |
| 直接场景 | GOAI 赛道一「新智基座｜Agent Infra」· 选题方向三：软件研发全流程协同 |
| 本地路径 | `D:\Run2AGI\` |
| 代码仓库 | `D:\Run2AGI\codentum\`（08-02 建立，将推 Gitee） |
| 设计文档 | `D:\Run2AGI\codentum\docs\`（00 总体设计 / 01 角色与Skill / 02 实施路线 / 03 团队分工 + `adr\`）★ 已从 Method 移入仓库 |
| ✅ 方案选型 | 六套并行方案已横评定案：**Codentum 102/120 选定为主干**，并吸收其余五套的 14 项机制。见 `Method\方案横评与选型决策.md` |
| 状态目录约定 | 生成的项目内用 `.codentum/` 存四张图与状态 |
| 团队 | **3 人**，用 Agent 协同开发（不是人工逐行写） |
| 与其他项目的关系 | 与 OpenHex（实习）、AiYing（自己的 OPC）**同级**，是第三条主线 |

### 命名

**Codentum = Code + Momentum**。Code 锚定软件开发赛道；Momentum 传递自驱动、自进化的持续成长势能，暗合多智能体协同的 1+1>2 涌现动力。
附带隐喻：动量 = 质量 × 速度且无外力时守恒 —— 能力沉淀是质量，交付速度是速度，L4 评测锚点保证已获得的能力不会在后续迭代中流失。

---

## 二、中心主张（一切设计的源头）

> **多 Agent 系统的可靠性来自不变量，不来自提示词。**
> 凡是"在提示词里叮嘱 Agent 不要做 X"的地方，都应改成"在结构上让 X 做不到"。

推论：**把不可恢复的部分交给确定性代码，把可恢复的部分交给模型。**
控制平面（锁、状态机、准入校验、门禁）出错不可恢复 → 一行 LLM 都不用；
执行平面（写代码、评审）出错可恢复（打回重试）→ 交给模型。

约束实现的优先级：**不可见 > 无权限 > 被拦截 > 提示词劝阻**。

---

## 三、六条不变量（骨架，不可破）

| ID | 名称 | 内容 |
|---|---|---|
| I1 | 单写者 | 任一路径同一时刻只被一个 in_progress packet 拥有 |
| I2 | 验收可判定 | 每个 packet ≥1 条机器可判定的验收谓词 |
| I3 | 契约冻结 | 只有 Architect 能写 `contracts/`，Coder 只读挂载 |
| I4 | 绿线 | main 任何时刻可构建可部署 E2E 通过 |
| I5 | 单会话闭合 | 一个 packet 必须在一个会话预算内完成，干不完就拆 |
| I6 | 证据 | 状态推进必须附证据引用，声明不算 |

---

## 四、架构速查

```
控制平面（确定性，无 LLM）  状态机 · 锁管理 · 准入校验 · 门禁 · 预算 · Guardian
执行平面（模型）            Intake·Architect·Planner·QA·Coder×N·Helper
                            Reviewer·Integrator·Manager·Evolver
数据平面                    四张图（依赖/所有权/溯源/知识）· Git 仓库 · Artifact · 三层记忆
上下文平面                  Context Broker（可见性矩阵 · 配方 · 预算降级链）
进化平面                    证伪门 · 影子回放 · Skill CI · 灰度（受"不动点"约束）
```

**同构参照**：控制平面 ≈ Kubernetes。`.codentum/` ≈ etcd，reconcile loop ≈ controller manager，
WorkPacket ≈ Pod spec，准入校验器 ≈ admission controller（此项对应精确），门禁 ≈ rollout gate。

### 双运行模式（共用同一套控制平面，一行代码不改）

- **Solo**：本地单进程 + 本地 Worker + 本地 Git + SQLite。零外部依赖，装完就跑
- **Team**：跑在 AgentTeams 上（Manager-Workers + Matrix + Higress + MinIO + PG）

四个接口抽象：`WorkerRuntime` / `ArtifactStore` / `MemoryIndex` / `ModelGateway`。**P1 就定死，之后只加实现不改签名**——否则双模式必然分叉。

---

## 五、已定的工程决策

| # | 决策 | 结论 | 定于 |
|---|---|---|---|
| 1 | 桌面壳 | **Electron**（Tauri 留到 P7 再评估） | 2026-08-01 |
| 2 | Solo 模式 Worker | **复用成熟编码 Agent，不自研执行体** | 2026-08-01 |
| 3 | 首个验证项目 | **全新小应用（订阅记账工具）** | 2026-08-01 |
| 4 | 项目命名 | **Codentum**（曾用 Loom、改善工场 KaizenWorks） | 2026-08-01 |
| 5 | 文档结构 | 收敛为 3 份，旧稿入 `历史设计/` | 2026-08-01 |
| 6 | **模型 Provider** | **阿里云百炼（Qwen）为默认 + Anthropic 等价可切换**（ADR-0005） | 2026-08-04 |
| 7 | 多 Agent 协同底座 | **AgentTeams** —— 赛题硬要求，对齐点是 `WorkerRuntime`（Team 模式） | 2026-08-04 |

---

## 六、分期与当前位置

| 期 | 目标 | 交付判据 | 状态 |
|---|---|---|:-:|
| **P0 骨架** | 控制平面 + WorkPacket + 单角色 | 手工写的 packet 能被认领→执行→验收→合入，状态变更在 Git 可见 | 🔨 进行中 |
| P1 编制 | 5 角色 + 并行 + 契约先行 + 依赖通道 | 3 packet 真并行零冲突；一次依赖请求被自动策略拦截并给替代 | ⬜ |
| P2 闭环 | 全阶段 + Provisioning + 冷启动 | 一句话 → 可交付应用 | ⬜ |
| P3 界面 | 桌面壳 + 核心视图 + 人类输入通道 | 全程不碰命令行 | ⬜ |
| P4 进化 | 钩子 A/B + 记忆注入 | 一条规则从失败中长出来并生效 | ⬜ |
| P5 度量 | 对照实验 + holdout | 进化增益曲线 | ⬜ |
| P6 强化 | 证伪门·影子回放·Helper·静默失效观测 | 坏 Skill 被门禁挡住并一键降级 | ⬜ |
| P7 产品 | 安装包·自动更新·Team 模式 | 可分发桌面软件 | ⬜ |

**当前位置：P0 进行中。** A 已完成路径锁与状态机；B 已完成 RoleSpec 加载、工具面派生、挂载表派生、Worker / Git Worktree 隔离最小壳、一次执行准备阶段串联、最小证据目录、Context Broker 最小壳、checkpoint-0 输入快照、`spawn(req)` 唯一入口收敛、本地命令 Runner 最小适配器、Runtime 配置/工厂入口、Worker Prompt Bundle、CommandRunner Prompt 输入占位符、ModelGatewayRunner 最小模型调用适配器、百炼 / Anthropic Provider 具体实现、真实 API smoke 工具壳、百炼真实 API smoke 实跑与可用模型清单探测，并完成 AgentTeams / HiClaw 本机环境预检；下一步按实测模型清单修正模型路由 / RoleSpec 隔离策略，再跑 AgentTeams 安装验证，并和 A 的准入校验器、落盘图状态对齐。

---

### 平台事项状态（2026-08-04 用户确认）

| 项 | 状态 |
|---|---|
| GOAI 官网报名 | ✅ 已完成 |
| 钉钉答疑群 186080014742 | ✅ 已加入 |
| 阿里云账号 | ✅ 已有；百炼 API Key 已用于本机 smoke（不入库、不落盘） |
| 代码仓库可访问 | ✅ Gitee 已上线 |
| **AgentTeams 安装验证** | 🟡 **环境预检完成，实跑阻塞于 LLM API Key** —— Docker Desktop/Compose 可用，当前无 AgentTeams 容器，`:18088` / `:18001` 未占用 |

---

## 七、赛程节点

| 阶段 | 截止 | 提交物 | 对应产品期 |
|---|---|---|---|
| **初赛** | **8.16** | 作品简介(≤500字) + 方案 PPT +（可选）代码包 | P0–P1 |
| 初赛评审 | 8.24 | Top30 进复赛 | — |
| **复赛** | 9.3 | 可执行代码包 + 可运行 Demo/视频 | P2 |
| 复赛评审 | 9.10 | Top15 进决赛 | — |
| **决赛** | 9.22 | 路演 PPT + 现场 Demo + 最终仓库 | P3–P4 |

### 🔴 红线

赛题原文：**仅 PPT / 概念方案、无可验证 PoC / 日志 / 视频，直接淘汰或大幅扣分**。
所以初赛真实目标 = **一段 3 分钟真实录屏 + 一张真实对照数据图**，PPT 是它俩的说明书。

### 初赛收敛目标（只证明三件事）

1. ≥3 角色的真实研发闭环跑得起来（真实 Git 提交、真实测试结果）
2. 契约先行的并行是真的（3 packet 同时跑零冲突，展示 `locks.json`）
3. 约束是结构性的（录到一次 Coder 试图写 `contracts/` 被文件系统拒绝）

进化部分初赛**只讲设计不承诺跑通**（赛题明确初赛重点评估方向与可行性）。

---

## 八、目录结构

```
D:\Run2AGI\
├── HistoricTask\                  ← 每完成一个任务写一份（同 OpenHex 规范）
│
├── codentum\                      ← ★ 代码仓库（已上 Gitee）
│   ├── README.md                   六条不变量 · 目录地图 · 第 0 周清单
│   ├── CONTRIBUTING.md             日常协作 8 条
│   ├── boundaries.yaml             ★ 团队路径独占表（与系统内部机制同构）
│   ├── .gitattributes              ★ *.sh eol=lf —— 不删，否则容器里 bad interpreter
│   ├── .gitignore / LICENSE        凭证排除 / Apache-2.0
│   ├── docs\                       ★ 设计文档已从 Method 移入此处（单一来源）
│   │   ├── 项目进展与记忆.md        ← ★ 本文件（08-04 从仓库外移入，随代码一起版本化）
│   │   ├── 00-总体设计方案.md       26 章，工程实施唯一依据
│   │   ├── 01-角色详细设计与Skill清单.md
│   │   ├── 02-实施路线与交付计划.md
│   │   ├── 03-团队分工与协作规范.md
│   │   ├── 04-模型路由表.md         ★ 初版，待实测修正
│   │   ├── Codentum技术栈总览.md    含 §十一 赛题规范对齐
│   │   └── adr\                    0001 仓库结构 · 0002 语言（已被 0003 取代）
│   │                               0003 Python 引擎 · 0004 LangGraph 适用层
│   │                               0005 模型 Provider · 🔴 0006 币种（待发起）
│   ├── packages\                   contracts A · control-plane A · harness B
│   │                               roles B · delivery C · desktop C
│   ├── fixtures\              A    golden-state 快照 —— C 的解耦点
│   ├── tests\                      contract（自动生成）· e2e
│   ├── docker\                C    cold-start · team-mode · delivery
│   └── scripts\               A    gen-contract-tests · check-boundaries · secret-scan
│
├── Method\                        ← 六套并行方案（✅ 已横评选型，作为证据链保留）
│   ├── 方案横评与选型决策.md        十二维度横评，Codentum 102/120 选定
│   ├── Codentum\                   设计文档已移入仓库；此处留指针 + PPT + 历史设计
│   ├── AppDevForge\ DevPlex Studio\ ForgePilot\ Loom\ Nexus\   另外五套
├── 赛题资料\                       官方分册 1–6
├── 赛道一…参赛手册.md / .pdf
├── 工具下载与平台注册清单.md
└── 应用软件开发完整闭环指南.md      ★ 六阶段门禁表是 Gate-as-Code 的编译源

C:\Users\宿心\Desktop\
├── Codentum设计评价与自我认知报告.md   设计的深度评价 + 认知画像 + 停止规则
└── Codentum基础设施与部署路线.md      Gitee + 本地 Docker → 云服务器
```

**⚠ 设计文档已移入 `codentum\docs\`，不再放 `Method\Codentum\`。**
移而不是复制——两份会分叉，而"文档与实现脱节"正是这套设计要消灭的问题之一。文档跟着代码走，同一个 commit 里改。

**⚠ 另外五套方案不要删。** 它们是选型的证据链——决赛若被问"考虑过其他架构吗"，六套并行方案 + 一份横评是很有力的回答。

---

## 九、最近任务

| 日期 | 任务 | 结果 |
|---|---|---|
| 08-01 | 赛题研读 + 系统设计（9 轮迭代） | 产出 3 份设计文档，从参赛叙事收敛到工程设计 |
| 08-01 | 文档收敛 8→3 份，改名 Codentum | 旧稿入 `历史设计/`，`.loom/` → `.codentum/` |
| 08-01 | 设计评价与自我认知报告 | 桌面 `Codentum设计评价与自我认知报告.md` |
| 08-01 | **六套方案横评与选型** | Codentum 102/120 选定；产出 `Method\方案横评与选型决策.md` |
| 08-01 | **14 项机制整合进主干** | 见下方 §十四 |
| 08-02 | 三人分工方案 + 基础设施路线 | `docs\03-团队分工与协作规范.md`；桌面《Codentum基础设施与部署路线》 |
| 08-02 | **代码仓库骨架搭建** | `D:\Run2AGI\codentum\`：48 目录 + 21 份 README/规范；设计文档移入 `docs\`；ADR-0001 已接受 |
| 08-02 | **语言选型定案：全 TypeScript** | ADR-0002 已接受。决定性理由：静态类型 = 白送的确定性验证器 |
| 08-02 | **契约初稿** | 四个接口签名（WorkerRuntime/ArtifactStore/MemoryIndex/ModelGateway）+ 五份 schema + 状态类型 bootstrap；tsc 编译通过 |
| 08-02 | **3 个 golden-state 快照** | empty / mid-flight / blocked，各带 NOTE.md |
| 08-02 | **gen-types 落地，契约债还清** | `state/index.ts` 由 schema 生成，幂等已验；`--check` 可拦手改；漏登记 schema 会报错而非静默兜底 |
| 08-02 | **★ 第 0 周完成：契约冻结** | 8 份 schema 过审冻结 · 4 个接口签名 · 38 个生成类型 · 105 个契约测试 · 6 个 package 全部编译通过 · `frozen_at = 2026-08-02`。四个工程脚本全部零依赖：gen-types · gen-contract-tests · check-boundaries · secret-scan。过审改掉 4 处（PacketState 重复 enum / 审计链哨兵格式不一 / reasonCode 无 pattern / 裸 string 该用 $ref）。校验抓到 1 处真问题（blocked 快照引用了不存在的证据） |
| 08-02 | 技术栈总览文档 | 桌面《Codentum技术栈总览.md》 |
| 08-03 | **★ 技术栈推翻重定：Python 核心引擎** | ADR-0003 取代 0002，ADR-0004 定 LangGraph 适用层。全仓迁移完成：5 个 Python 包 + 1 个 TS 包；`gen_types.py` 一份 schema 发两套类型（Pydantic + TS）；4 个工具脚本全部移植为 Python 且零依赖；105 个契约测试改走 pytest，每条走 schema + Pydantic **两条**校验路径。`make verify-offline` 全绿。桌面技术栈文档已重写 |
| 08-03 | **★ P0 开工第一块：路径锁前缀树（I1 强制点）** | `packages/control-plane/.../locks/`。按路径分段建树，一次 O(深度) 下降同时判祖先/同路径/后代三种冲突。归一化是 I1 的第一道门（`a/b`≡`a/b/`≡`./a/b`≡`a//b`≡`a\b`；大小写用 casefold —— Windows/macOS 上 `Src/App.py` 与 `src/app.py` 是同一个文件，当成两条就在主力平台上直接破掉 I1）。两层并发控制：进程内互斥锁 + 跨进程 version 乐观锁。136 条通过（105 契约 + 31 新增），五项零依赖检查全绿。**⚠ 见 §十五：并发测试第一版是空转的** |
| 08-04 | **★ 技术栈补齐赛题规范对齐（§十一）** | 依据《工具下载与平台注册清单》。**补上一条此前完全缺失的硬要求**：手册原文「多 Agent 协同设计必须以 AgentTeams 为设计基点」——架构上不用改（Team 模式本就跑在它上面，对齐点是 `WorkerRuntime`），缺的是**把它写出来**。七项推荐生态逐项表态：Higress ✅ / 云 Skills ✅ / AgentLoop·LoongSuite 🟡 / Nacos ⚠️Solo 不用 / PolarDB ❌ / **RocketMQ ❌**。★ 拒绝 RocketMQ 恰恰是在演示核心主张（Agent 之间不直接通信，协同发生在控制平面），讲好了是加分项不是缺失项 |
| 08-04 | **★ ADR-0005：模型 Provider 改用阿里云百炼** | 百炼（Qwen 系）为默认，Anthropic 保留为**等价可切换实现**。原本写 anthropic 不是权衡后的选择，是**没把赛题生态算进去**。保留第二实现的首要理由：让「等价契约切换开关」有了真实两端 —— 现场切一次 Provider 两边跑通，是录屏里**看得见**的动作。`ModelGateway` 签名一个字没改，恰好验证抽象有效。**🔴 由此暴露契约冲突，见 §十六** |

| 08-04 | **模型路由表初版 + 样例需求定案** | `docs/04-模型路由表.md`：排序原则 = **按「错了有多贵」分档，不按「活有多难」**（architect 出错传播面最大→最强模型；coder 可恢复但量最大→中档省钱）。★ 拟表时发现契约漏了两条同类硬约束：**helper ≠ coder**（否则求助退化成再问自己一遍）与 **qa ≠ coder**（角色隔离做到了、错误去相关没做到），已提给 B。⚠️ 具体模型 id 与各角色表现**均无数据**，须拿到百炼 Key 后核对。样例需求定为**日常规划工具**（任务/日记/鼓励语三模块天然并行），按 fixtures README 的规矩拆成人话版 `REQUIREMENT.md` + 标尺版 `ACCEPTANCE.md` |
| 08-04 | **★ P0 第二块：WorkPacket 状态机** | `state_machine/transitions.py`。**转换表从 RoleSpec 派生，模块内没有任何写死的表** —— 有专门一条测试盯这个（换一份 RoleSpec 后同一转换必须从允许变拒绝）。强制 I6（缺证据直接拒）、guardian.usesModel=false（schema 明说要代码强制）、终态不得转出（加载期就拒）。三个写错了还不报错的地方已堵：**rejected 不是终态**（写进去所有被打回的 packet 会悄悄卡死）· 自环被拒（I6 的洞）· 同一转换两个门禁则拒绝加载。155 条测试通过 |
| 08-04 | **★ B P0 切片 1：RoleSpec 加载 + 工具面派生** | `packages/roles/codentum_roles/loader.py` + `packages/harness/codentum_harness/tool_surface/`。新增 `coder` / `reviewer` / `qa` 三个最小 RoleSpec；Harness 从 `RoleSpec.tools` 派生工具面，未授权工具**不出现在列表里**，不是调用时才拒绝。新增 B 侧测试并纳入 pytest 默认收集；`make verify-offline` 从 155 条增至 162 条，全绿。 |
| 08-04 | **★ B P0 切片 2：RoleSpec 挂载表派生** | `packages/harness/codentum_harness/mounts/`。`writes → rw`、`reads → ro`、`invisible → 不挂载`；同一路径 ro/rw 重叠、父子 ro/rw 重叠、`invisible` 被已挂载父目录暴露、`../` 路径穿越均 fail-closed。调整 reviewer RoleSpec，避免 `evidence/**` 父目录挂载暴露 Coder reasoning。`make verify-offline` 从 162 条增至 168 条，全绿。 |
| 08-04 | **Codex 项目 Skill 路由加载** | 本项目当前优先使用 4 个技能：`ai-agent-development`（Agent/Harness 总流程）、`agent-orchestration-multi-agent-optimize`（并行、成本、可靠性指标）、`architecture`（边界/ADR/取舍）、`agent-tool-builder`（工具 schema、错误处理、工具面设计）。未加载 `agent-memory-systems` / `agent-evaluation` 等大技能；等做到记忆层、评测门禁时再按需加载，避免上下文膨胀。 |
| 08-04 | **★ B P0 切片 3：Worker / Git Worktree 隔离最小壳** | `packages/harness/codentum_harness/worker/`。新增 `GitWorktreeManager` 与 `LocalWorkerRuntime`：`spawn()` 在模型阶段前创建仓库外部的 detached git worktree，拒绝把 worker workspace 放进控制器 checkout 内；事件流可按 seq 回放；真实模型执行先用可注入 `runner` 占位，保持 P0 边界清晰。新增 5 条 worker 测试；B 侧目标 ruff/mypy 通过；`make verify-offline` 从 168 条增至 173 条，全绿。 |
| 08-05 | **★ B P0 切片 4：Harness 准备阶段串联 + 最小证据目录** | `packages/harness/codentum_harness/prepare/` 新增 `prepare_spawn_request()`，把 `RoleSpec.tools`、挂载派生、模型路由、预算、workspace 与 attempt 组装成冻结契约里的 `SpawnRequest`，不触碰控制平面状态机。`LocalWorkerRuntime.spawn()` 现在会在 worker worktree 内写 `.codentum/evidence/<worker_id>/manifest.json` 与 `events.jsonl`，记录 worker、packet、role、attempt、工具面、挂载表和启动/结束事件。新增 3 条测试；B 侧测试 21 条通过，目标 ruff/mypy 通过；`make verify-offline` 从 173 条增至 176 条，全绿。 |
| 08-05 | **★ B P0 切片 5：Context Broker 最小壳** | `packages/harness/codentum_harness/context_broker/`。新增 `assemble_context_bundle()`：输入 RoleSpec、候选上下文切片与字符预算，先按 `RoleSpec.invisible` 剔除不可见 artifact 并记录 `denied`，再按全文 → summary → reference 的固定降级链装配，禁止随机截断；必需上下文若被隐藏或连 reference 都放不下则 fail-closed。`prepare_spawn_request()` 现在可携带 `ContextBundle`，但仍不改冻结的 `SpawnRequest` 签名。新增 7 条测试；B 侧测试 28 条通过；`make verify-offline` 从 176 条增至 183 条，全绿。 |
| 08-06 | **★ B P0 切片 6：checkpoint-0 输入快照** | `packages/harness/codentum_harness/checkpoint/`。新增 `write_initial_checkpoint()`，把 `SpawnRequest` 与可选 `ContextBundle` 序列化成稳定 JSON，写入 `.codentum/evidence/<worker_id>/checkpoints/0000.json`，并返回 `CheckpointRef(seq=0, digest=sha256:...)`。digest 不含时间戳，同输入跨目录写入保持一致；上下文变化会改变 digest。`LocalWorkerRuntime.spawn()` 现在会写 checkpoint 事件。新增 3 条测试；B 侧测试 31 条通过；`make verify-offline` 从 183 条增至 186 条，全绿。 |
| 08-06 | **★ B P0 切片 6b：`spawn(req)` 唯一入口收敛** | A/B 确认冻结接口 `WorkerRuntime.spawn(req)` 原样不动，控制平面只看到这一个入口。B 将上一版公开辅助入口 `spawn_prepared(prepared)` 收回：`LocalWorkerRuntime.spawn(req)` 内部先 `_prepare(req) -> PreparedExecution`，加载内置 RoleSpec，并在配置了 `context_loader` 时调用 Context Broker 装配 `ContextBundle`；随后 `_spawn(prepared)` 创建 git worktree、写 manifest、写 checkpoint-0 与事件流。`PreparedExecution` 仍是 harness 内部类型，不进 contracts。B 侧测试 31 条通过；目标 ruff/mypy 通过；`make verify-offline` 186 条全绿。 |
| 08-06 | **★ B P0 切片 7：本地命令 Runner 最小适配器** | `packages/harness/codentum_harness/runner/`。新增 `CommandRunner`，把任何 argv 形式的本地命令包装成 `WorkerRunner`：命令在 worker workspace 内执行，支持 `{packet_id}`、`{attempt}`、`{workspace}`、`{worker_id}`、`{role}`、`{evidence_dir}` 占位符，不走 shell 拼接；stdout/stderr/result 固定写入 `.codentum/evidence/<worker_id>/runner/`，并以 `file:runner/result.json` 返回证据。成功时从 `git status` 派生 `touched_paths`，失败、超时、命令不存在会转成冻结契约里的 `WorkerFailed`。这一步先打通真实执行适配口，还没有绑定百炼 / Hermes / Claude Code 的模型循环。新增 3 条测试；B 侧测试 34 条通过；目标 ruff/mypy 通过；`make verify-offline` 从 186 条增至 189 条，全绿。 |
| 08-06 | **★ B P0 切片 8：Runtime 配置/工厂入口** | `packages/harness/codentum_harness/runtime.py`。新增 `RunnerConfig`、`LocalWorkerRuntimeConfig`、`build_runner()`、`build_local_worker_runtime()`：产品入口现在可以用配置组装出带 Runner 的 `LocalWorkerRuntime`，而不是在各处手写构造细节。当前支持 `none` 与 `command` 两种 runner kind，非法空命令和非正 timeout 会 fail-fast；后续接百炼 / Hermes / Claude Code 时只扩展 runner 构建逻辑，不改冻结的 `WorkerRuntime.spawn(req)` 接口。新增 3 条测试；B 侧测试 37 条通过；`make verify-offline` 从 189 条增至 192 条，全绿。 |
| 08-06 | **★ B P0 切片 9：Worker Prompt Bundle** | `packages/harness/codentum_harness/prompt_bundle/`。新增 `assemble_worker_prompt_bundle()` 与 `write_worker_prompt_bundle()`，把已经强制过的 RoleSpec / SpawnRequest / ContextBundle 稳定渲染为 `system.md`、`user.md` 与 `prompt/manifest.json`，digest 可复现，并可转换成冻结契约里的 `ModelRequest`。`LocalWorkerRuntime.spawn(req)` 现在会在 runner 启动前写出 `.codentum/evidence/<worker_id>/prompt/`；runner 可直接读取这份输入包。测试覆盖：同输入跨目录 digest 稳定、隐藏上下文正文不进入 prompt、role/context 不匹配 fail-fast、runner 启动前 prompt 已落盘。B 侧测试 41 条通过；目标 ruff/mypy 通过；`make verify-offline` 从 192 条增至 196 条，全绿。 |
| 08-06 | **★ B P0 切片 10：CommandRunner Prompt 输入占位符** | `packages/harness/codentum_harness/runner/command.py`。在原有 `{packet_id}`、`{workspace}`、`{evidence_dir}` 等 argv 占位符基础上，新增 `{prompt_dir}`、`{system_prompt}`、`{user_prompt}`、`{prompt_manifest}`，外部编码 Agent 命令现在可以直接读取上一切片落盘的 Prompt Bundle，不需要自己拼 `.codentum/evidence/<worker_id>/prompt/` 路径。新增 1 条测试，证明命令可通过 `{user_prompt}` 读取 `user.md`；B 侧测试 42 条通过；目标 ruff/mypy 通过；`make verify-offline` 从 196 条增至 197 条，全绿。 |
| 08-09 | **★ B P0 切片 11：ModelGatewayRunner 最小模型调用适配器** | `packages/harness/codentum_harness/runner/model_gateway.py`。新增 `ModelGatewayRunner`，把冻结契约里的 `ModelGateway` 包装成 `WorkerRunner`：从 `.codentum/evidence/<worker_id>/prompt/` 加载并校验 Prompt Bundle digest，打开一次模型会话，调用 `ModelRequest`，把 `response.txt`、`usage.json`、`tool_calls.json`、`result.json` 写入 `model/` 证据目录。`prompt_bundle` 同步新增 `load_worker_prompt_bundle()`，防止 Runner 自己猜 prompt 文件格式。当前是 one-shot 模型调用，不含工具循环；`tool_use`、`refusal`、`max_output` 会 fail-closed 成 `WorkerFailed(model_error)`。新增 4 条测试；B 侧测试 46 条通过；目标 ruff/mypy 通过；`make verify-offline` 从 197 条增至 201 条，全绿。 |
| 08-09 | **★ AgentTeams / HiClaw 安装验证预检** | 使用 `ai-agent-development`、`agent-orchestration-multi-agent-optimize`、`architecture`、`bash-linux` 口径做本机预检。结论：Docker CLI `28.3.2`、Docker Compose `v2.38.2-desktop.1` 已可用；已启动 Docker daemon；当前只发现 `open-webui` 容器，无 `agentteams` / `hiclaw` / `higress` / `matrix` / `minio` 相关容器或卷；`:18088` 与 `:18001` 端口空闲。官方安装脚本需要 LLM Provider / API Key / 管理员配置，本机 `AGENTTEAMS_LLM_API_KEY`、`DASHSCOPE_API_KEY`、`BAILIAN_API_KEY`、`QWEN_API_KEY`、`OPENAI_API_KEY`、`ANTHROPIC_API_KEY` 均未配置，所以没有用假 Key 继续安装。下一步：拿到百炼 Key 后按官方 AgentTeams 安装脚本实跑，并验证 `docker ps | grep hiclaw-manager`、Element Web `:18088`、Higress `:18001`。 |
| 08-09 | **★ B P0 切片 12：百炼 / Anthropic Provider 具体实现** | `packages/harness/codentum_harness/model_gateway/`。新增 `OpenAICompatibleGateway`、`BailianOpenAICompatibleGateway`、`AnthropicGateway`、`TokenPricing` 与 `ModelGatewayPolicy`：百炼走 OpenAI-compatible chat completions，默认 `https://dashscope.aliyuncs.com/compatible-mode/v1`，API Key 从 `DASHSCOPE_API_KEY` / `BAILIAN_API_KEY` / `QWEN_API_KEY` / `AGENTTEAMS_LLM_API_KEY` 读取；Anthropic 走 Messages API。两侧都把 Provider 响应归一化为冻结契约里的 `ModelResponse` / `Usage` / `ToolCall`，费用按显式 token price table 计算，缺价格默认 fail-closed，避免模型调用被当成 0 成本。`runtime.py` 同步扩展 `ModelGatewayConfig` 与 `RunnerConfig.model_gateway()`，产品入口可以通过配置组装 `ModelGatewayRunner`，不把密钥值写入配置。新增 8 条 Provider / factory 测试；B 侧相关测试 14 条通过；目标 ruff/mypy 通过；`make verify-offline` 从 201 条增至 209 条，全绿。 |
| 08-09 | **★ B P0 切片 13：真实 API smoke 工具壳** | `packages/harness/codentum_harness/model_gateway/smoke.py`。新增可用 `python -m codentum_harness.model_gateway.smoke` 运行的 Provider smoke：通过 `ModelGatewayConfig` 构造百炼 / OpenAI-compatible / Anthropic 网关，打开一次会话，发送极小提示，输出不含密钥的 JSON 摘要（provider、session、model、role、stop_reason、usage、ledger_total_usd、text_preview）。默认必须显式传模型价格；除非加 `--allow-unknown-pricing`，否则缺价格直接 fail-closed，避免 smoke 把真实调用记成 0 成本。本机仍无百炼 Key，所以只完成工具与 fake-gateway 测试，真实 API smoke 等 Key 后执行。新增 4 条 smoke 测试；B 侧相关测试 15 条通过；目标 ruff/mypy 通过；`make verify-offline` 从 209 条增至 213 条，全绿。 |
| 08-09 | **★ B P0 切片 14：百炼真实 API smoke + 模型清单探测** | 使用用户提供的百炼 CSV（只本机读取，未打印/提交 Key）提取 `apiKey` 与 OpenAI-compatible endpoint，安装本地 `openai` SDK 后通过 `BailianOpenAICompatibleGateway` 实跑 `qwen-plus` smoke。结果：`stop_reason=end`，返回 `codentum-smoke-ok`，usage 为 input 45 / output 6；本次因价格表未核对，显式按 unknown pricing 跑连通性验证，`cost_usd=0` 不作为成本证据。模型列表接口可用，返回 236 个模型；与当前路由表相关候选包括 `qwen-plus` / `qwen-max` / `qwen-coder-plus` / `deepseek-v4-pro` / `deepseek-v4-flash` / `kimi-k2.6` / `kimi/kimi-k2.6` / `glm-5.2` / `ZHIPU/GLM-5.2` 等。下一步：补官方价格表或证据化价格配置，再把路由表里的模型 id 改成账号实测可调值。 |
| 08-09 | **★ B P0 切片 15：RoleSpec 模型路由实测修正** | `packages/roles/specs/` 按当前百炼业务空间**真实可调**结果修正默认模型：`coder → qwen-coder-plus-1106`，`reviewer → qwen-plus-2025-11-05`，`qa → qwen-plus-2025-07-14`。三者均通过 `BailianOpenAICompatibleGateway` 真实 smoke，均返回 `codentum-smoke-ok`，usage 均为 input 45 / output 6；仍按 unknown pricing 只作连通性证据，不作成本证据。探测中发现 `kimi/kimi-k2.6` 与 `ZHIPU/GLM-5.2` 当前业务空间未开通产品，不能写成默认路由；`qwen-max` 能返回文本但不是标准 `choices` 形态且 `usage=null`，所以 Provider parser 增加百炼 `text` 回退，但在已配置价格时遇到缺 usage 仍 fail-closed。`ModelGatewayPolicy` 的家族识别同步修正为先剥离 provider namespace（如 `siliconflow/deepseek-v3.2` 归到 `deepseek` 家族），避免后续跨供应商模型隔离判断偏掉。新增测试覆盖百炼 text 回退、缺 usage 的 priced fail-closed、namespace 家族归一化、内置 RoleSpec 的 reviewer/qa 不复用 coder 默认模型。目标 ruff/mypy 通过；`make verify-offline` 从 213 条增至 217 条，全绿。 |

**已推 Gitee**：`6e13e44`（路径锁）· `f723fb0`（赛题对齐）· `a3981b1`（ADR-0005）
· `53ba29d`（模型路由表 + 样例需求 + 状态机）。推前均跑过 secret-scan（工作区 + git 历史）。

**已推 Gitee（B / `feat/harness`）**：`96cd62a`（RoleSpec 加载、工具面派生、挂载表派生、B 侧测试与 `pyproject.toml` 测试收集配置）· Worker / Git Worktree 隔离最小壳 + Harness 准备阶段串联 + 最小证据目录（B P0 切片 3-4；提交 hash 以 `git log` 为准）· Context Broker 最小壳（B P0 切片 5；提交 hash 以 `git log` 为准）· checkpoint-0 输入快照 + `spawn(req)` 唯一入口收敛（B P0 切片 6-6b；提交 hash 以 `git log` 为准）· 本地命令 Runner 最小适配器 + Runtime 配置/工厂入口（B P0 切片 7-8；提交 hash 以 `git log` 为准）· Worker Prompt Bundle + CommandRunner Prompt 输入占位符 + ModelGatewayRunner 最小模型调用适配器 + 百炼 / Anthropic Provider 具体实现 + 真实 API smoke 工具壳 + 百炼真实 smoke 与模型清单探测（B P0 切片 9-14；提交 hash 以 `git log` 为准）。

---

## 十、待办

1. ✅ ~~六套方案横向选型~~ —— 已完成（08-01），Codentum 选定并完成 14 项机制整合
2. ✅ ~~仓库骨架~~ —— 已完成（08-02），`D:\Run2AGI\codentum\`
3. ✅ ~~语言与运行时选型~~ —— ⚠️ **08-03 推翻重定**：
   **Python 核心引擎 + TS 桌面端**（ADR-0003，取代 ADR-0002）。
   决定性理由是**团队三人 Python 熟、TS 生疏**，距初赛 15 天。
   ADR-0002 的技术论证本身没错，错在把「团队对两种语言成本相同」这个**从没验证过的假设**当成了已知。
   代价已明确记录：失去 `tsc` 免费门禁（用 mypy strict + `ignore-without-code` + Pydantic 补）·
   跨语言契约（两侧都生成，schema 唯一真源）· Electron+PyInstaller 打包（⚠️ 最大执行风险，P1 必须打通）
3b. ✅ **LangGraph 用在执行平面，不用在控制平面**（ADR-0004）。
   「四张图」是数据结构，LangGraph 的 Graph 是控制流图，不是一回事。
   ★ 边界判据：把 LangGraph 整个换掉，控制平面应当一行都不用改
4. ✅ ~~`scripts/gen-types`~~ —— 已完成（08-02）。`state/index.ts` 现在是真生成物，
   `pnpm gen:check` 可拦手改。附带完成 `scripts/validate-fixtures`（schema + 四项交叉检查）。
   **两个脚本都零运行时依赖，`pnpm install` 之前就能跑。**
5. ✅ ~~**第 0 周 = 契约冻结**~~ —— 已完成（08-02）。`boundaries.yaml: frozen_at = 2026-08-02`
   ⚠️ 若 B/C 还没实际过目 8 份 schema + 4 个接口签名，把 `frozen_at` 改回 `null` 走完确认再填
6. ✅ ~~`git init`~~ —— 已完成，仓库已上 Gitee，I3 与 secret-scan 的历史扫描均已生效
7. 🔨 **P0 进行中** —— 今天是 2026-08-09，距初赛截止 **7 天**（2026-08-16）。A 的 P0 主线是**三件**
   （`docs/03` 写死的）：状态机 + 路径锁 + 准入校验器
   - **A · 路径锁 ✅**（`6e13e44`）—— I1 的强制点
   - **A · 状态机 ✅**（`53ba29d`）—— 转换表从 RoleSpec 派生，强制 I6
   - **A · 准入校验器 ⬜** ← **下一件**
   - **A · 附带：写 C 的 secret-scan 验收测试 ⬜** —— QA-first 的制衡，`boundaries.yaml`
     注明「实现简单但后果严重」。⚠️ 讽刺的是这是我们自己设计的机制，第一次轮到自己用还没做
   - **B · Harness 准备阶段 ✅（工具面 + 挂载表 + Worker / Worktree 隔离 + SpawnRequest 准备 + 最小证据目录 + Context Broker 最小壳 + checkpoint-0 + `spawn(req)` 唯一入口）** ｜ B 真实 Runner 最小适配器 ✅ ｜ B Runtime 工厂入口 ✅ ｜ B Worker Prompt Bundle ✅ ｜ B CommandRunner Prompt 输入 ✅ ｜ B ModelGatewayRunner ✅ ｜ B 百炼 / Anthropic Provider 具体实现 ✅ ｜ B 真实 API smoke 工具壳 ✅ ｜ B 真实 API smoke 实跑 ✅ ｜ B 模型路由实测修正 ✅（Qwen 可调第一版；跨家族隔离待产品开通） ｜ C 三快照渲染看板 ⬜
   P0 判据：一个手工写的 packet 能被认领 → 执行 → 验收 → 合入，状态变更在 Git 可见
   **判据要三件合起来才成立** —— 锁只解决「认领时不撞车」，状态机解决「怎么推进」，
   还差「凭什么放行」（准入校验器）与**落盘到 `.codentum/graph.json`**
7b. 🟡 **AgentTeams 安装验证预检完成，实跑阻塞于 LLM API Key** —— 赛题**唯一的硬要求**（清单里成员 B 的事项）。
   已确认 Docker Desktop / Compose 可用，Docker daemon 已启动，当前无 AgentTeams 相关容器或卷，`:18088` / `:18001` 端口空闲；官方安装仍需要百炼 / 通义千问或等价 Provider 的 API Key，拿到 Key 后再跑安装脚本并验证 `docker ps | grep hiclaw-manager` · Element Web `:18088` · Higress `:18001`
   ⚠️ 现状从「完全没跑过」推进到「环境可安装，缺模型 Key 不能实跑」；不要在汇报里写成已完成 AgentTeams 安装
7c. ✅ **百炼 API Key 已可用，本机 smoke 已跑通** —— `qwen-plus` 通过 OpenAI-compatible endpoint 返回 `codentum-smoke-ok`；模型列表接口返回 236 个模型；`coder/reviewer/qa` 三个默认模型已按账号实测改成可调 Qwen 固定版本，并跑过第一轮对照 smoke。
   仍待做：补价格表证据；确认是否开通 Kimi / GLM / DeepSeek 后再把 `mustDifferFrom` 从“具体模型不同”提升为“模型家族不同”
7d. 🔴 **发起 ADR-0006（成本口径币种）** —— 见 §十六。契约冻结字段写死 usd，百炼计人民币。
   已写了一份给非技术读者的说明放桌面：`Codentum-ADR0006说明-钱该用什么单位记.md`
7e. 🟡 **模型路由表 §四 的三条建议落地进度** —— `qa ≠ coder` 已在内置 RoleSpec + Policy 测试里覆盖；`mustDifferFrom` 的家族比较代码已支持 provider namespace 归一化，但默认未开启，因为当前业务空间只实测通过 Qwen 系列；`helper ≠ coder` 仍待新增 helper RoleSpec 或等 Helper 角色进入执行链时落地。
8. ✅ ~~定样例需求~~ —— 已定（08-04）：**日常规划工具**（记每日任务 + 更新完成情况 +
   写日记 + 按完成情况给正向反馈）。任务/日记/鼓励语三模块**语义上本来就无关**，
   正常 Planner 也会这么切 —— 不是刻意拆出来的并行。鼓励语要读任务完成数据，
   天然带一条跨模块依赖，正好当 P1「依赖请求被拦截并给替代」的素材。
   否决了带认证的记账（认证的复杂度落在错误的地方，且破坏冷启动测试）与团队待办（要服务端）
9. 🟡 定图表库（甘特 + DAG）—— 前端唯一真正的选型。建议 dagre 布局 + 自绘 SVG。
   见桌面《Codentum技术栈总览.md》§四的四条判据
10. ⚠️ **推 Gitee 前必须先跑 secret-scan，包括 git 历史**（不只是工作区）——自己的产品自己先用
11. 补设计文档的**适用边界**：对已理解领域最强、对探索性项目最弱
12. 决定**问题定义端（Intake）是加厚还是明标为已知薄弱**
13. 统一自进化对外口径：*"解决的是让进化可度量可回滚不失控；让进化更有效仍是开放问题"*
14. 参赛材料**向 ForgePilot 的密度靠拢**（它 410 行讲清了 91 分的内容；Codentum 的 4000 行是开发指导不是参赛材料）

---

## 十一、评审已知的五处经验性未知

设计无法解决、只能跑起来才知道的：

1. **契约变更频率** —— 若过高，串行变更窗口会成为新瓶颈，并行度塌回 1
2. **小样本进化增益** —— 一个项目 20 个 packet，"同类失败 ≥2 次"的聚类可能寥寥无几
3. **最小有效任务尺度** —— 六条不变量的仪式成本对小任务可能超过收益，未定义旁路
4. **模型能力窗口** —— 大量脚手架的前提是模型上下文与自主性有限；**不变量层不依赖模型能力，是最抗时间的部分**
5. **需求定义端的薄弱** —— Intake 相对执行端薄一个数量级，而真实浪费主要来自建错东西

---

## 十二、任务文档规范（同 OpenHex）

**每完成一个任务，在 `D:\Run2AGI\HistoricTask\` 写一份文档。**
任务粒度 = 一个可交付能力（如「控制平面 P0 骨架」「Provisioning 表单」），不是单个 commit。

必含三节：

1. **问题 / 需求分析** —— 原始诉求，真正的问题在哪
2. **解决 / 实现措施** —— 怎么做的，为什么这么做
3. **经验教训** —— 踩了什么坑，下次怎么避免

文件名 `<任务名>-<YYYYMMDD>.md`。**写实情**：未验证的部分标明，不要把「代码写完」说成「已验证可用」。

---

## 十三、给这个项目的停止规则

来自设计评价报告。**这套规则不许在遇到阻力时被自己放宽**（进化的不动点同理适用于人）。

```
□ 每个方向开工前先写三条 wont_have，写不出来说明边界没想清楚
□ 每件事必须能写出"什么算做完了"，不接受"做得好"这类表述
□ 超过两周看不到可演示产出的方向，强制拆或砍，不许"再坚持一下"
□ "这个有价值"不算证据；有人用了并给了反馈才算
□ 每个方向定时间预算，超 120% 强制三选一：追加 / 缩范围 / 终止
□ 参赛材料只用产品已有的真实产出，不为演示写一次性代码
```

**已知的最大风险**（不是技术）：范围膨胀。九轮设计对话中提出的需求全是加法，零减法。
三人 + Agent 移除了"一个人做不完"这堵反馈墙，膨胀的代价将不再自动显现。
六套并行方案本身就是这个模式的一次表现——好在已经收敛为一套。

---

## 十四、六套方案横评结论与整合清单

**排名**：Codentum 102 > ForgePilot 91 > Nexus 82 > DevPlex 75 > Loom 69 > AppDevForge 60（满分 120，十二维度）

**Codentum 与 ForgePilot 是同一层级，其余四份低一档。**选 Codentum 的三条理由：唯一有可证伪中心主张 · 唯一让并行度可计算 · 异常路径写得比正常路径厚。

**⚠ 利益冲突已声明**：Codentum 由本人（Claude）参与产出，横评报告开篇已列三条自查措施，并明确指出 ForgePilot 在三个维度上更强。

### 已整合的 14 项机制

| 来源 | 机制 | 落点 |
|---|---|---|
| **ForgePilot** | Git Worktree 物理隔离（与路径锁**互补非二选一**） | `00 · 状态设计` |
| ForgePilot | Skill 五态状态机 + 五条红线 | `00 · Skill 体系`、`01` |
| ForgePilot | 检索的确定性梯度（结构化事实→决策→契约→代码索引→向量→原始日志） | `00 · 上下文工程` |
| **Nexus** | 命令粒度权限（`forbidden_commands` / `network_access`） | Skill Manifest |
| Nexus | **审计哈希链** `sha256(prev + payload)` | `00 · 审计链` |
| Nexus | **对抗性测试套件**（主动越权以验证护栏本身） | `00 · 自进化`、`01` |
| Nexus | 执行证据包五类产物（含工具调用输入输出哈希） | `00 · 输出契约` |
| Nexus | 影子回放用过去 3 个真实项目数据 | `00 · 进化流水线` |
| **DevPlex** | 等价契约切换开关（把可迁移性变成可演示动作） | `00 · MCP 面板` |
| DevPlex | Skills 面板四块划分 | `00 · 可视化` |
| **Loom** | RARE-V 循环命名（Reason/Act/Repair/Verify/Evolve） | `00 · 内部规划` |
| Loom | Skill `conflicts` 互斥字段 | Skill Manifest |
| Loom | Peer-Debug（L0.5，同角色另一实例） | `00 · 分级求助`、`01` |
| **AppDevForge** | 阿里云官方云 Skills 清单（7 个） | `01 · 云 Skills` |
| AppDevForge | AgentTeams 四道安全防线表述 | `00 · 约束对齐·三` |

### 明确否掉的一条

DevPlex 的"一级进化：参数与 Prompt 自动调优，**无需人工审核，后台静默执行**"。
**静默的能力变更 = 无法归因的退化。**效果退化是静默的，等发现时已无法定位是哪次调优造成的。

### 一条执行提醒

**参赛材料要向 ForgePilot 的密度靠拢。** 它 410 行做到 91 分，Codentum 4037 行做到 102 分——按每行信息密度算 ForgePilot 更高。Codentum 的体量是**开发指导文档**，PPT 和作品简介必须压缩。评审看密度不看篇幅。

---

## 十五、写第一块代码时踩到的：并发测试默认是空转的

**这条值得单列，因为它直接威胁「我们靠不变量而不是提示词」这个中心主张的可信度。**

路径锁写完后，先做了一次「这个测试真的能失败吗」的验证：把 `LockTable`
的互斥锁整个换成空操作，跑 **400 轮**五线程抢重叠路径 ——
**依然每轮都恰好一个赢家，一次都没红。**

原因是临界区太短，GIL 在 check-then-act 之间根本没机会切走。
也就是说：那条名义上验证 I1 的并发用例，**在锁被完全删掉的情况下照样全绿**。

把 `sys.setswitchinterval` 压到 1µs 后，无锁版本立刻暴露。已作为 autouse
fixture 固化进用例并写明理由。

### 三条推论

1. **并发测试不主动制造穿插，测的就只是「串行调用两次」。**
   写并发用例时必须配一次「把锁摘掉，它会不会红」的对照实验，
   否则那条用例的绿灯与「有没有锁」无关。

2. **这与 I2「验收可判定」是同一个问题的两面。**
   我们要求每个 packet 的验收谓词机器可判定 —— 但「可判定」不等于
   「判得出差别」。一条永远返回 true 的谓词也是机器可判定的。
   ★ 准入校验器将来要考虑：能否要求验收谓词附带一个「已知失败样本」，
   证明它在错误输入上确实为假。**没有这一步，I2 挡不住空转的验收。**

3. 顺带抓到实现里一个真 bug：`next(iter(...))` 抛的 StopIteration 会被
   Python 转成 `RuntimeError: generator raised StopIteration`，堆栈指向
   调用点，真实原因（计数器与树不一致）被完全掩盖。已改成显式抛出。
   —— 这个 bug 只有在无锁压测里才会现形，串行测试永远碰不到。

---

## 十六、🔴 待发起：ADR-0006 成本口径的币种（由 ADR-0005 带出）

`interfaces.py`（第 0 周**已冻结**）里成本字段全部以 usd 命名：

```
BudgetGrantRuntime.limit_usd · WorkerCompleted/Failed/Aborted.spent_usd
Usage.cost_usd（"预算判断一律看 cost_usd"）· ModelGateway.open(grant_usd=...)
```

**而阿里云百炼以人民币计价。**

### 为什么不能「实现时再说」

设计里「预算一律用货币，不用 token」的理由写得很清楚：token 口径会
**静默失真**，且失真方向随路由变化、事后查不出来。

**一个写死的汇率会把同一类失真原样请回来** —— 汇率变动不报错，只让账本慢慢偏，
偏离方向随汇率走。用货币替代 token 就是为了消灭这类失真，
转头在货币里塞一个硬编码汇率，这条设计约束就自我抵消了。

> ★ 不是假想风险。2026-08-03 在 OpenHex 项目里刚踩过一模一样的：一段
> 「按 ¥7/USD 从美元价换算」的定价代码，**从引入那天起就偏 1.5~2%，
> 偏了一个月没人发现** —— 因为它不报错。而那还是有官方人民币公布价可查的情况。

### 三个可选解

| 方案 | 代价 |
|---|---|
| A 汇率转换 | ❌ 正是上面那个坑。若非要走，汇率**必须记进证据包**，让它可审计而不是隐形 |
| **B 改契约为币种标注**（`Money{amount, currency}` 或改名） | 契约变更，需三人同意 + ADR + 变更窗口 —— **但这正是那个流程存在的理由** |
| C 认定 usd 是抽象记账单位 | 最省事，但名字与含义不符，正是这套设计最反对的漂移 |

### 立场

**倾向 B，且不由 A 单方面执行。**

I3 的规矩是「只有 A 能写 contracts，改动需三人同意 + 新 ADR + 走变更窗口」。
A 以「反正我是 owner」为由绕过自己定的流程，**比字段名错了更糟** ——
整套论点建立在「约定靠不住，约束才可靠」上，第一次遇到不便就自己松一次，
之后这条约束对谁都不再成立。

**与 Provider 决策不互相阻塞**：币种影响预算账本的准确度，不影响能否调通模型。
