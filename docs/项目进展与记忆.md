# Run2AGI / Codentum 项目进展与记忆

> 最后更新：2026-08-12（接入现状已逐项核对，见 §二十五 · 红线只剩录屏）
> 每次涉及本项目的任务都更新此文件；Agent 启动时读取此文件恢复上下文
>
> ⚠️ **08-10 修复了一次记录丢失**：08-09 的两次 merge（`46f1dca` / `3ea9997`）
> 在解冲突时把 A 当天写的 85 行记录整段丢掉了 —— 包括「P0 真实达成」的状态、
> 四处静默缺陷的排查记录、以及 §十七 整节。已从 `ba7490f` 取回并与 B 的新内容合并。
> **教训见 §十八。**

---

## ⚠️ 这份文件的定位：**派生视图，不是真源**

**本文仅供团队协作与进度同步参考。具体改动一律以事实文件为准。**

它是一份**方便人读的摘要**，用来快速恢复上下文、看清现在做到哪儿了。
它会滞后、会遗漏、也可能写错。**当它与事实文件冲突时，事实文件对，本文错。**

### 事实文件分别是哪些

| 你想知道的事 | 唯一真源 | 别看这里 |
|---|---|---|
| 代码实际怎么做的 | **源码与测试** | 本文的描述 |
| 数据长什么形状 | `packages/contracts/schemas/*.json` | 本文，或任何 md |
| 接口签名 | `packages/contracts/python/…/interfaces.py`（手写，非生成物） | 本文 |
| 谁能写哪些路径 | **`boundaries.yaml`** | 本文 §八 的目录树 |
| 角色权限 / 状态转换表 / 工具面 | **RoleSpec**（`packages/roles/**`） | 任何手写的表 |
| 某个决策为什么这么定 | **`docs/adr/`** | 本文的一句话摘要 |
| 到底发生过什么、谁改的 | **`git log`** | 本文的「最近任务」表 |

> ★ 这不是一句免责声明，是把本项目的核心主张用在自己身上：
> **同一件事有两个地方可查，它们迟早会不一致，而不一致时通常不报错。**
> 所以每件事都必须指定唯一真源，其余一律是派生视图 —— 本文就是派生视图。

### 发现不一致时怎么办

**改本文去对齐事实文件，不要改事实文件去迁就本文。**

如果确实是事实文件错了，那就改事实文件 —— 但那是一次真正的变更，
要走它自己的流程（契约走变更窗口、代码过测试与门禁），
而不是先在这里改一笔，让两边看起来一致了事。

---

## 一、项目概况

| 项 | 内容 |
|----|------|
| 项目名 | **Codentum**（Code + Momentum） |
| 一句话 | 自进化软件开发多 Agent 系统；一句话需求 → 可交付应用 |
| 产品形态 | **本地优先的桌面软件**，对标 OpenClaw / OpenWorker / WorkBuddy |
| 直接场景 | GOAI 赛道一「新智基座｜Agent Infra」· 选题方向三：软件研发全流程协同 |
| 本地路径 | `D:\Run2AGI\` |
| 代码仓库 | `D:\Run2AGI\codentum\`（08-02 建立，将推 Gitee） |
| 设计文档 | `D:\Run2AGI\codentum\docs\`（00 总体设计 / 01 角色与Skill / 02 实施路线 / 03 团队分工 + `adr\`）★ 已从 Method 移入仓库 |
| ✅ 方案选型 | 六套并行方案已横评定案：**Codentum 102/120 选定为主干**，并吸收其余五套的 14 项机制。见 `Method\方案横评与选型决策.md` |
| 状态目录约定 | 生成的项目内用 `.codentum/` 存四张图与状态 |
| 团队 | **3 人**，用 Agent 协同开发（不是人工逐行写） |
| 与其他项目的关系 | 与 OpenHex（实习）、AiYing（自己的 OPC）**同级**，是第三条主线 |

### 命名

**Codentum = Code + Momentum**。Code 锚定软件开发赛道；Momentum 传递自驱动、自进化的持续成长势能，暗合多智能体协同的 1+1>2 涌现动力。
附带隐喻：动量 = 质量 × 速度且无外力时守恒 —— 能力沉淀是质量，交付速度是速度，L4 评测锚点保证已获得的能力不会在后续迭代中流失。

---

## 二、中心主张（一切设计的源头）

> **多 Agent 系统的可靠性来自不变量，不来自提示词。**
> 凡是"在提示词里叮嘱 Agent 不要做 X"的地方，都应改成"在结构上让 X 做不到"。

推论：**把不可恢复的部分交给确定性代码，把可恢复的部分交给模型。**
控制平面（锁、状态机、准入校验、门禁）出错不可恢复 → 一行 LLM 都不用；
执行平面（写代码、评审）出错可恢复（打回重试）→ 交给模型。

约束实现的优先级：**不可见 > 无权限 > 被拦截 > 提示词劝阻**。

---

## 三、六条不变量（骨架，不可破）

| ID | 名称 | 内容 |
|---|---|---|
| I1 | 单写者 | 任一路径同一时刻只被一个 in_progress packet 拥有 |
| I2 | 验收可判定 | 每个 packet ≥1 条机器可判定的验收谓词 |
| I3 | 契约冻结 | 只有 Architect 能写 `contracts/`，Coder 只读挂载 |
| I4 | 绿线 | main 任何时刻可构建可部署 E2E 通过 |
| I5 | 单会话闭合 | 一个 packet 必须在一个会话预算内完成，干不完就拆 |
| I6 | 证据 | 状态推进必须附证据引用，声明不算 |

---

## 四、架构速查

```
控制平面（确定性，无 LLM）  状态机 · 锁管理 · 准入校验 · 门禁 · 预算 · Guardian
执行平面（模型）            Intake·Architect·Planner·QA·Coder×N·Helper
                            Reviewer·Integrator·Manager·Evolver
数据平面                    四张图（依赖/所有权/溯源/知识）· Git 仓库 · Artifact · 三层记忆
上下文平面                  Context Broker（可见性矩阵 · 配方 · 预算降级链）
进化平面                    证伪门 · 影子回放 · Skill CI · 灰度（受"不动点"约束）
```

**同构参照**：控制平面 ≈ Kubernetes。`.codentum/` ≈ etcd，reconcile loop ≈ controller manager，
WorkPacket ≈ Pod spec，准入校验器 ≈ admission controller（此项对应精确），门禁 ≈ rollout gate。

### 双运行模式（共用同一套控制平面，一行代码不改）

- **Solo**：本地单进程 + 本地 Worker + 本地 Git + SQLite。零外部依赖，装完就跑
- **Team**：跑在 AgentTeams 上（Manager-Workers + Matrix + Higress + MinIO + PG）

四个接口抽象：`WorkerRuntime` / `ArtifactStore` / `MemoryIndex` / `ModelGateway`。**P1 就定死，之后只加实现不改签名**——否则双模式必然分叉。

---

## 五、已定的工程决策

| # | 决策 | 结论 | 定于 |
|---|---|---|---|
| 1 | 桌面壳 | **Electron**（Tauri 留到 P7 再评估） | 2026-08-01 |
| 2 | Solo 模式 Worker | **复用成熟编码 Agent，不自研执行体** | 2026-08-01 |
| 3 | 首个验证项目 | **全新小应用（订阅记账工具）** | 2026-08-01 |
| 4 | 项目命名 | **Codentum**（曾用 Loom、改善工场 KaizenWorks） | 2026-08-01 |
| 5 | 文档结构 | 收敛为 3 份，旧稿入 `历史设计/` | 2026-08-01 |
| 6 | **模型 Provider** | **阿里云百炼（Qwen）为默认 + Anthropic 等价可切换**（ADR-0005） | 2026-08-04 |
| 7 | 多 Agent 协同底座 | **AgentTeams** —— 赛题硬要求，对齐点是 `WorkerRuntime`（Team 模式） | 2026-08-04 |
| 8 | **控制平面 reconcile** | 调和循环（ReconcileLoop）：依赖检查·锁获取·worker调度·门禁判定·状态落盘·崩溃恢复。26 测试 | 2026-08-05 |
| 9 | **控制平面 admission** | 准入校验器（AdmissionChecker）：7 条规则，I2 自评审禁止·路径·预算·DAG·角色存在·模型隔离。29 测试。与 B 的 RoleSpec 对接 | 2026-08-05 |
| 10 | **控制平面 gates** | 门禁执行器（GateRunner）：4 内置门禁（evidence/self-test/acceptance/review）。19 测试。与 B 的 requiresGate 对接 | 2026-08-05 |
| 11 | **控制平面 budget** | 预算追踪器（BudgetTracker）：全局支出·按角色/模型分摊·三级告警（80%/95%/100%）。20 测试。集成到 reconcile | 2026-08-05 |
| 12 | **控制平面 guardian** | 确定性拦截器（Guardian）：I3 契约冻结·I5 会话闭合·拦截记录。15 测试。集成到 reconcile | 2026-08-05 |
| 13 | **B harness/roles 启动** | feat(harness): derive role tools and mounts — 14 文件 +604 行，feat/harness 分支 | 2026-08-05 |
| 14 | **C desktop 启动** | feat(desktop): add interactive operations console — 36 文件 +12,583 行，role-c/desktop-operations 分支 | 2026-08-05 |
| 15 | **C 桌面端审查** | A 完成深度审查报告，指出 v1 74% 代码偏离设计。C 代码从 main 撤销 | 2026-08-06 |
| 16 | **B WorkerRuntime 完整** | feat/harness 8 个提交：LocalWorkerRuntime + ContextBroker + Checkpoint + PromptBundle + CommandRunner。合入 main | 2026-08-06 |
| 17 | **A+B 联调** | ReconcileLoop + LocalWorkerRuntime 接口验证通过，DeepSeek API 直接测试成功 | 2026-08-06 |
| 18 | **货币切换 CNY** | 全项目 USD→CNY：Schema 重生成、39 文件、306 测试通过 | 2026-08-07 |
| 19 | **C desktop v2 重写** | 完全重写：RequirementComposer + ProjectStateSource + 8 视图 + NO_FAKE_SHELL_CHECKLIST + delivery 全线。91 文件 +16,334 行，合入 main | 2026-08-08 |
| 20 | **三人交叉核对** | A↔B、A↔C、B↔C 接口全部对齐，411 测试通过，零冲突 | {today} |

---

## 六、分期与当前位置

| 期 | 目标 | 交付判据 | 状态 |
|---|---|---|:-:|
| **P0 骨架** | 控制平面 + WorkPacket + 单角色 | 手工写的 packet 能被认领→执行→验收→合入，状态变更在 Git 可见 | ✅ **08-09 真实达成** —— worker 真的改了代码、git 可见、有真实证据、被验收；判据由 `tests/e2e/TestRealExecution` 守着 |
| P1 编制 | 5 角色 + 并行 + 契约先行 + 依赖通道 | 3 packet 真并行零冲突；一次依赖请求被自动策略拦截并给替代 | ⬜ |
| P2 闭环 | 全阶段 + Provisioning + 冷启动 | 一句话 → 可交付应用 | ⬜ |
| P3 界面 | 桌面壳 + 核心视图 + 人类输入通道 | 全程不碰命令行 | ⬜ |
| P4 进化 | 钩子 A/B + 记忆注入 | 一条规则从失败中长出来并生效 | ⬜ |
| P5 度量 | 对照实验 + holdout | 进化增益曲线 | ⬜ |
| P6 强化 | 证伪门·影子回放·Helper·静默失效观测 | 坏 Skill 被门禁挡住并一键降级 | ⬜ |
| P7 产品 | 安装包·自动更新·Team 模式 | 可分发桌面软件 | ⬜ |

**当前位置：P0 真实达成（08-09）；模型层端到端已首次验证（08-10）；B 侧已补任务意图注入最小闭环，并让明确 blocker 报告 fail-closed；完整任务描述字段仍待 ADR-0007。**

A 的控制平面 7/7 模块已进 main；B 的 Harness 已完成 WorkerRuntime、ContextBroker、Checkpoint、PromptBundle、CommandRunner、ModelGatewayRunner、百炼/Anthropic Provider、真实 API smoke 与 RoleSpec 模型路由实测修正；C v2 桌面端已合入 main。**08-10 的三方集成、真实模型 e2e、护栏消融实验与桌面工作台更新已全部进入 Gitee main（当前 `origin/main = 327c0fb`）。**

⚠️ **08-08 那句「A+B 闭环贯通」当时是假的** —— 它建立在一个缺陷上：验收兜底分支
只判「evidence 非空」，而 packet 手上那条 `sys:lock:` 是控制面自己写的簿记，
于是 worker 失败也被标成 accepted。08-09 修掉后真实情况才显露。详见 §十七。

⚠️ **08-10 新识别并补上了一条口径问题**：模型层已有完整管道（provider + runner + smoke），
但组件测试使用假 gateway，真实 API smoke 只证明「能连上」。随后新增
`tests/e2e/test_real_model_execution.py`，让真实 `qwen-coder-plus-1106`
在 `ReconcileLoop` 内跑完一个 packet。它证明了**真实模型调用、用量落盘、证据进入验收链路**；
同时也暴露了两个更深缺陷：WorkPacket 没有任务描述字段，且 blocker 报告会被当成交付物验收。B 已先在 Harness 侧补最小 `packet-intent` 上下文，避免模型再收到空的 `Visible Context`；随后补了 `ModelGatewayRunner` 明确 blocker 报告检测，让这类自陈失败转成 `WorkerFailed(acceptance_not_met)`。完整任务描述字段仍需 ADR-0007 决定。

剩下的风险已收敛到可演示性：**录屏 + 对照数据图**仍是唯一出局项；
RoleSpec 与 prompts 已补到 11/11，但 skills 仍未落地；AgentTeams Team-mode `WorkerRuntime`
已有资源/证据适配壳，`status.json` 也已投影进 Worker 事件流，执行中心可以按现有
`manifest.json` / `events.jsonl` 链路展示 Team-mode Worker 状态；任务派发与结果回收仍未接入。

---

### 平台事项状态（2026-08-04 用户确认）

| 项 | 状态 |
|---|---|
| GOAI 官网报名 | ✅ 已完成 |
| 钉钉答疑群 186080014742 | ✅ 已加入 |
| 阿里云账号 | ✅ 已有；百炼 API Key 已用于本机 smoke（不入库、不落盘） |
| 代码仓库可访问 | ✅ Gitee 已上线 |
| **AgentTeams 安装验证** | ✅ **08-10 本机实跑完成** —— 使用官方 AgentTeams 安装脚本启动 `agentteams-controller` / `agentteams-manager` / `agentteams-dashboard`；Element Web `:18088`、Higress Console `:18001`、Dashboard `:13000`、Manager Console `:18888` 均返回 200。随后用官方 `agt` CLI 创建 `coder` Worker，资源与容器均为 Running。截图证据见 `docs/experiments/agentteams-install/`。本地凭据在 `~/agentteams-manager.env`，不入库 |
| **控制平面** | ✅ 7/7 模块完成，控制平面单测 158 条通过 |
| **P0 真实执行** | ✅ 08-09 达成，`tests/e2e/TestRealExecution` 守着 |
| **模型层端到端** | ✅ **08-10 首次跑通** —— 真实 `qwen-coder-plus-1106` 在 ReconcileLoop 内跑完一个 packet 并被验收，由 `tests/e2e/test_real_model_execution.py` 守着。⚠️ 同时暴露两个新缺陷，见待办 22 |
| **B 进度** | ✅ WorkerRuntime + ContextBroker + Checkpoint + PromptBundle + CommandRunner + ModelGatewayRunner + 百炼/Anthropic Provider + 真实 smoke 已完成；RoleSpec 已补齐 11/11，`packages/roles/prompts/` 已补 11 份 prompt，PromptBundle 会读取 `promptRef`；LocalWorkerRuntime 会自动注入 `packet-intent` 上下文；明确 blocker 报告会 fail-closed；TeamWorkerRuntime 已能通过 AgentTeams CLI 创建/检查 Worker 资源并写证据，且会把 `agentteams/status.json` 同步投影成 Worker `progress` 事件供执行中心展示；任务派发与结果回收仍 fail-closed |
| **C 进度** | ✅ v2 重写完成并合入 main：RequirementComposer + 8 视图 + delivery 全线，8,212 行 |
| **交叉核对** | ✅ A↔B↔C 三向接口全部对齐；08-09 首次有**真测试**覆盖接缝（含跨语言的 A→C 交接）；B 的 provider/route 已 merge 并推 main |
| **测试总量** | 本机 `make verify-offline`：Python 415 passed / 4 skipped，secret-scan 扫工作区与 git 历史通过；desktop TS 31 条记录沿用 C 侧最新口径。08-10 B 已补 `test_prompt_bundle.py` 的显式 `encoding="utf-8"`，不再让 Prompt Bundle 测试依赖本机 locale |
| **总代码量** | 20,381 行（A 6,932 / 34% · B 5,263 / 26% · C 8,186 / 40%）。口径：A = contracts py + control-plane + tests/；B = harness + roles；C = desktop ts + delivery py。另计 scripts 1,812 行、contracts TS 408 行 |
| **更新时间** | 2026-08-10 |

---

## 七、赛程节点

| 阶段 | 截止 | 提交物 | 对应产品期 |
|---|---|---|---|
| **初赛** | **8.16** | 作品简介(≤500字) + 方案 PPT +（可选）代码包 | P0–P1 |
| 初赛评审 | 8.24 | Top30 进复赛 | — |
| **复赛** | 9.3 | 可执行代码包 + 可运行 Demo/视频 | P2 |
| 复赛评审 | 9.10 | Top15 进决赛 | — |
| **决赛** | 9.22 | 路演 PPT + 现场 Demo + 最终仓库 | P3–P4 |

### 🔴 红线

赛题原文：**仅 PPT / 概念方案、无可验证 PoC / 日志 / 视频，直接淘汰或大幅扣分**。
所以初赛真实目标 = **一段 3 分钟真实录屏 + 一张真实对照数据图**，PPT 是它俩的说明书。

### 初赛收敛目标（只证明三件事）

1. ≥3 角色的真实研发闭环跑得起来（真实 Git 提交、真实测试结果）
2. 契约先行的并行是真的（3 packet 同时跑零冲突，展示 `locks.json`）
3. 约束是结构性的（录到一次 Coder 试图写 `contracts/` 被文件系统拒绝）

进化部分初赛**只讲设计不承诺跑通**（赛题明确初赛重点评估方向与可行性）。

---

## 八、目录结构

```
D:\Run2AGI\
├── HistoricTask\                  ← 每完成一个任务写一份（同 OpenHex 规范）
│
├── codentum\                      ← ★ 代码仓库（已上 Gitee）
│   ├── README.md                   六条不变量 · 目录地图 · 第 0 周清单
│   ├── CONTRIBUTING.md             日常协作 8 条
│   ├── boundaries.yaml             ★ 团队路径独占表（与系统内部机制同构）
│   ├── .gitattributes              ★ *.sh eol=lf —— 不删，否则容器里 bad interpreter
│   ├── .gitignore / LICENSE        凭证排除 / Apache-2.0
│   ├── docs\                       ★ 设计文档已从 Method 移入此处（单一来源）
│   │   ├── 项目进展与记忆.md        ← ★ 本文件（08-04 从仓库外移入，随代码一起版本化）
│   │   ├── 00-总体设计方案.md       26 章，工程实施唯一依据
│   │   ├── 01-角色详细设计与Skill清单.md
│   │   ├── 02-实施路线与交付计划.md
│   │   ├── 03-团队分工与协作规范.md
│   │   ├── 04-模型路由表.md         ★ 初版，待实测修正
│   │   ├── Codentum技术栈总览.md    含 §十一 赛题规范对齐
│   │   └── adr\                    0001 仓库结构 · 0002 语言（已被 0003 取代）
│   │                               0003 Python 引擎 · 0004 LangGraph 适用层
│   │                               0005 模型 Provider · 🔴 0006 币种（待发起）
│   ├── packages\                   contracts A · control-plane A · harness B
│   │                               roles B · delivery C · desktop C
│   ├── fixtures\              A    golden-state 快照 —— C 的解耦点
│   ├── tests\                      contract（自动生成）· e2e
│   ├── docker\                C    cold-start · team-mode · delivery
│   └── scripts\               A    gen-contract-tests · check-boundaries · secret-scan
│
├── Method\                        ← 六套并行方案（✅ 已横评选型，作为证据链保留）
│   ├── 方案横评与选型决策.md        十二维度横评，Codentum 102/120 选定
│   ├── Codentum\                   设计文档已移入仓库；此处留指针 + PPT + 历史设计
│   ├── AppDevForge\ DevPlex Studio\ ForgePilot\ Loom\ Nexus\   另外五套
├── 赛题资料\                       官方分册 1–6
├── 赛道一…参赛手册.md / .pdf
├── 工具下载与平台注册清单.md
└── 应用软件开发完整闭环指南.md      ★ 六阶段门禁表是 Gate-as-Code 的编译源

C:\Users\宿心\Desktop\
├── Codentum设计评价与自我认知报告.md   设计的深度评价 + 认知画像 + 停止规则
└── Codentum基础设施与部署路线.md      Gitee + 本地 Docker → 云服务器
```

**⚠ 设计文档已移入 `codentum\docs\`，不再放 `Method\Codentum\`。**
移而不是复制——两份会分叉，而"文档与实现脱节"正是这套设计要消灭的问题之一。文档跟着代码走，同一个 commit 里改。

**⚠ 另外五套方案不要删。** 它们是选型的证据链——决赛若被问"考虑过其他架构吗"，六套并行方案 + 一份横评是很有力的回答。

---

## 九、最近任务

| 日期 | 任务 | 结果 |
|---|---|---|
| 08-01 | 赛题研读 + 系统设计（9 轮迭代） | 产出 3 份设计文档，从参赛叙事收敛到工程设计 |
| 08-01 | 文档收敛 8→3 份，改名 Codentum | 旧稿入 `历史设计/`，`.loom/` → `.codentum/` |
| 08-01 | 设计评价与自我认知报告 | 桌面 `Codentum设计评价与自我认知报告.md` |
| 08-01 | **六套方案横评与选型** | Codentum 102/120 选定；产出 `Method\方案横评与选型决策.md` |
| 08-01 | **14 项机制整合进主干** | 见下方 §十四 |
| 08-02 | 三人分工方案 + 基础设施路线 | `docs\03-团队分工与协作规范.md`；桌面《Codentum基础设施与部署路线》 |
| 08-02 | **代码仓库骨架搭建** | `D:\Run2AGI\codentum\`：48 目录 + 21 份 README/规范；设计文档移入 `docs\`；ADR-0001 已接受 |
| 08-02 | **语言选型定案：全 TypeScript** | ADR-0002 已接受。决定性理由：静态类型 = 白送的确定性验证器 |
| 08-02 | **契约初稿** | 四个接口签名（WorkerRuntime/ArtifactStore/MemoryIndex/ModelGateway）+ 五份 schema + 状态类型 bootstrap；tsc 编译通过 |
| 08-02 | **3 个 golden-state 快照** | empty / mid-flight / blocked，各带 NOTE.md |
| 08-02 | **gen-types 落地，契约债还清** | `state/index.ts` 由 schema 生成，幂等已验；`--check` 可拦手改；漏登记 schema 会报错而非静默兜底 |
| 08-02 | **★ 第 0 周完成：契约冻结** | 8 份 schema 过审冻结 · 4 个接口签名 · 38 个生成类型 · 105 个契约测试 · 6 个 package 全部编译通过 · `frozen_at = 2026-08-02`。四个工程脚本全部零依赖：gen-types · gen-contract-tests · check-boundaries · secret-scan。过审改掉 4 处（PacketState 重复 enum / 审计链哨兵格式不一 / reasonCode 无 pattern / 裸 string 该用 $ref）。校验抓到 1 处真问题（blocked 快照引用了不存在的证据） |
| 08-02 | 技术栈总览文档 | 桌面《Codentum技术栈总览.md》 |
| 08-03 | **★ 技术栈推翻重定：Python 核心引擎** | ADR-0003 取代 0002，ADR-0004 定 LangGraph 适用层。全仓迁移完成：5 个 Python 包 + 1 个 TS 包；`gen_types.py` 一份 schema 发两套类型（Pydantic + TS）；4 个工具脚本全部移植为 Python 且零依赖；105 个契约测试改走 pytest，每条走 schema + Pydantic **两条**校验路径。`make verify-offline` 全绿。桌面技术栈文档已重写 |
| 08-03 | **★ P0 开工第一块：路径锁前缀树（I1 强制点）** | `packages/control-plane/.../locks/`。按路径分段建树，一次 O(深度) 下降同时判祖先/同路径/后代三种冲突。归一化是 I1 的第一道门（`a/b`≡`a/b/`≡`./a/b`≡`a//b`≡`a\b`；大小写用 casefold —— Windows/macOS 上 `Src/App.py` 与 `src/app.py` 是同一个文件，当成两条就在主力平台上直接破掉 I1）。两层并发控制：进程内互斥锁 + 跨进程 version 乐观锁。136 条通过（105 契约 + 31 新增），五项零依赖检查全绿。**⚠ 见 §十五：并发测试第一版是空转的** |
| 08-04 | **★ 技术栈补齐赛题规范对齐（§十一）** | 依据《工具下载与平台注册清单》。**补上一条此前完全缺失的硬要求**：手册原文「多 Agent 协同设计必须以 AgentTeams 为设计基点」——架构上不用改（Team 模式本就跑在它上面，对齐点是 `WorkerRuntime`），缺的是**把它写出来**。七项推荐生态逐项表态：Higress ✅ / 云 Skills ✅ / AgentLoop·LoongSuite 🟡 / Nacos ⚠️Solo 不用 / PolarDB ❌ / **RocketMQ ❌**。★ 拒绝 RocketMQ 恰恰是在演示核心主张（Agent 之间不直接通信，协同发生在控制平面），讲好了是加分项不是缺失项 |
| 08-04 | **★ ADR-0005：模型 Provider 改用阿里云百炼** | 百炼（Qwen 系）为默认，Anthropic 保留为**等价可切换实现**。原本写 anthropic 不是权衡后的选择，是**没把赛题生态算进去**。保留第二实现的首要理由：让「等价契约切换开关」有了真实两端 —— 现场切一次 Provider 两边跑通，是录屏里**看得见**的动作。`ModelGateway` 签名一个字没改，恰好验证抽象有效。**🔴 由此暴露契约冲突，见 §十六** |

| 08-04 | **模型路由表初版 + 样例需求定案** | `docs/04-模型路由表.md`：排序原则 = **按「错了有多贵」分档，不按「活有多难」**（architect 出错传播面最大→最强模型；coder 可恢复但量最大→中档省钱）。★ 拟表时发现契约漏了两条同类硬约束：**helper ≠ coder**（否则求助退化成再问自己一遍）与 **qa ≠ coder**（角色隔离做到了、错误去相关没做到），已提给 B。⚠️ 具体模型 id 与各角色表现**均无数据**，须拿到百炼 Key 后核对。样例需求定为**日常规划工具**（任务/日记/鼓励语三模块天然并行），按 fixtures README 的规矩拆成人话版 `REQUIREMENT.md` + 标尺版 `ACCEPTANCE.md` |
| 08-04 | **★ P0 第二块：WorkPacket 状态机** | `state_machine/transitions.py`。**转换表从 RoleSpec 派生，模块内没有任何写死的表** —— 有专门一条测试盯这个（换一份 RoleSpec 后同一转换必须从允许变拒绝）。强制 I6（缺证据直接拒）、guardian.usesModel=false（schema 明说要代码强制）、终态不得转出（加载期就拒）。三个写错了还不报错的地方已堵：**rejected 不是终态**（写进去所有被打回的 packet 会悄悄卡死）· 自环被拒（I6 的洞）· 同一转换两个门禁则拒绝加载。155 条测试通过 |
| 08-04 | **★ B P0 切片 1：RoleSpec 加载 + 工具面派生** | `packages/roles/codentum_roles/loader.py` + `packages/harness/codentum_harness/tool_surface/`。新增 `coder` / `reviewer` / `qa` 三个最小 RoleSpec；Harness 从 `RoleSpec.tools` 派生工具面，未授权工具**不出现在列表里**，不是调用时才拒绝。新增 B 侧测试并纳入 pytest 默认收集；`make verify-offline` 从 155 条增至 162 条，全绿。 |
| 08-04 | **★ B P0 切片 2：RoleSpec 挂载表派生** | `packages/harness/codentum_harness/mounts/`。`writes → rw`、`reads → ro`、`invisible → 不挂载`；同一路径 ro/rw 重叠、父子 ro/rw 重叠、`invisible` 被已挂载父目录暴露、`../` 路径穿越均 fail-closed。调整 reviewer RoleSpec，避免 `evidence/**` 父目录挂载暴露 Coder reasoning。`make verify-offline` 从 162 条增至 168 条，全绿。 |
| 08-04 | **Codex 项目 Skill 路由加载** | 本项目当前优先使用 4 个技能：`ai-agent-development`（Agent/Harness 总流程）、`agent-orchestration-multi-agent-optimize`（并行、成本、可靠性指标）、`architecture`（边界/ADR/取舍）、`agent-tool-builder`（工具 schema、错误处理、工具面设计）。未加载 `agent-memory-systems` / `agent-evaluation` 等大技能；等做到记忆层、评测门禁时再按需加载，避免上下文膨胀。 |
| 08-04 | **★ B P0 切片 3：Worker / Git Worktree 隔离最小壳** | `packages/harness/codentum_harness/worker/`。新增 `GitWorktreeManager` 与 `LocalWorkerRuntime`：`spawn()` 在模型阶段前创建仓库外部的 detached git worktree，拒绝把 worker workspace 放进控制器 checkout 内；事件流可按 seq 回放；真实模型执行先用可注入 `runner` 占位，保持 P0 边界清晰。新增 5 条 worker 测试；B 侧目标 ruff/mypy 通过；`make verify-offline` 从 168 条增至 173 条，全绿。 |
| 08-05 | **★ B P0 切片 4：Harness 准备阶段串联 + 最小证据目录** | `packages/harness/codentum_harness/prepare/` 新增 `prepare_spawn_request()`，把 `RoleSpec.tools`、挂载派生、模型路由、预算、workspace 与 attempt 组装成冻结契约里的 `SpawnRequest`，不触碰控制平面状态机。`LocalWorkerRuntime.spawn()` 现在会在 worker worktree 内写 `.codentum/evidence/<worker_id>/manifest.json` 与 `events.jsonl`，记录 worker、packet、role、attempt、工具面、挂载表和启动/结束事件。新增 3 条测试；B 侧测试 21 条通过，目标 ruff/mypy 通过；`make verify-offline` 从 173 条增至 176 条，全绿。 |
| 08-05 | **★ B P0 切片 5：Context Broker 最小壳** | `packages/harness/codentum_harness/context_broker/`。新增 `assemble_context_bundle()`：输入 RoleSpec、候选上下文切片与字符预算，先按 `RoleSpec.invisible` 剔除不可见 artifact 并记录 `denied`，再按全文 → summary → reference 的固定降级链装配，禁止随机截断；必需上下文若被隐藏或连 reference 都放不下则 fail-closed。`prepare_spawn_request()` 现在可携带 `ContextBundle`，但仍不改冻结的 `SpawnRequest` 签名。新增 7 条测试；B 侧测试 28 条通过；`make verify-offline` 从 176 条增至 183 条，全绿。 |
| 08-06 | **★ B P0 切片 6：checkpoint-0 输入快照** | `packages/harness/codentum_harness/checkpoint/`。新增 `write_initial_checkpoint()`，把 `SpawnRequest` 与可选 `ContextBundle` 序列化成稳定 JSON，写入 `.codentum/evidence/<worker_id>/checkpoints/0000.json`，并返回 `CheckpointRef(seq=0, digest=sha256:...)`。digest 不含时间戳，同输入跨目录写入保持一致；上下文变化会改变 digest。`LocalWorkerRuntime.spawn()` 现在会写 checkpoint 事件。新增 3 条测试；B 侧测试 31 条通过；`make verify-offline` 从 183 条增至 186 条，全绿。 |
| 08-06 | **★ B P0 切片 6b：`spawn(req)` 唯一入口收敛** | A/B 确认冻结接口 `WorkerRuntime.spawn(req)` 原样不动，控制平面只看到这一个入口。B 将上一版公开辅助入口 `spawn_prepared(prepared)` 收回：`LocalWorkerRuntime.spawn(req)` 内部先 `_prepare(req) -> PreparedExecution`，加载内置 RoleSpec，并在配置了 `context_loader` 时调用 Context Broker 装配 `ContextBundle`；随后 `_spawn(prepared)` 创建 git worktree、写 manifest、写 checkpoint-0 与事件流。`PreparedExecution` 仍是 harness 内部类型，不进 contracts。B 侧测试 31 条通过；目标 ruff/mypy 通过；`make verify-offline` 186 条全绿。 |
| 08-06 | **★ B P0 切片 7：本地命令 Runner 最小适配器** | `packages/harness/codentum_harness/runner/`。新增 `CommandRunner`，把任何 argv 形式的本地命令包装成 `WorkerRunner`：命令在 worker workspace 内执行，支持 `{packet_id}`、`{attempt}`、`{workspace}`、`{worker_id}`、`{role}`、`{evidence_dir}` 占位符，不走 shell 拼接；stdout/stderr/result 固定写入 `.codentum/evidence/<worker_id>/runner/`，并以 `file:runner/result.json` 返回证据。成功时从 `git status` 派生 `touched_paths`，失败、超时、命令不存在会转成冻结契约里的 `WorkerFailed`。这一步先打通真实执行适配口，还没有绑定百炼 / Hermes / Claude Code 的模型循环。新增 3 条测试；B 侧测试 34 条通过；目标 ruff/mypy 通过；`make verify-offline` 从 186 条增至 189 条，全绿。 |
| 08-06 | **★ B P0 切片 8：Runtime 配置/工厂入口** | `packages/harness/codentum_harness/runtime.py`。新增 `RunnerConfig`、`LocalWorkerRuntimeConfig`、`build_runner()`、`build_local_worker_runtime()`：产品入口现在可以用配置组装出带 Runner 的 `LocalWorkerRuntime`，而不是在各处手写构造细节。当前支持 `none` 与 `command` 两种 runner kind，非法空命令和非正 timeout 会 fail-fast；后续接百炼 / Hermes / Claude Code 时只扩展 runner 构建逻辑，不改冻结的 `WorkerRuntime.spawn(req)` 接口。新增 3 条测试；B 侧测试 37 条通过；`make verify-offline` 从 189 条增至 192 条，全绿。 |
| 08-06 | **★ B P0 切片 9：Worker Prompt Bundle** | `packages/harness/codentum_harness/prompt_bundle/`。新增 `assemble_worker_prompt_bundle()` 与 `write_worker_prompt_bundle()`，把已经强制过的 RoleSpec / SpawnRequest / ContextBundle 稳定渲染为 `system.md`、`user.md` 与 `prompt/manifest.json`，digest 可复现，并可转换成冻结契约里的 `ModelRequest`。`LocalWorkerRuntime.spawn(req)` 现在会在 runner 启动前写出 `.codentum/evidence/<worker_id>/prompt/`；runner 可直接读取这份输入包。测试覆盖：同输入跨目录 digest 稳定、隐藏上下文正文不进入 prompt、role/context 不匹配 fail-fast、runner 启动前 prompt 已落盘。B 侧测试 41 条通过；目标 ruff/mypy 通过；`make verify-offline` 从 192 条增至 196 条，全绿。 |
| 08-06 | **★ B P0 切片 10：CommandRunner Prompt 输入占位符** | `packages/harness/codentum_harness/runner/command.py`。在原有 `{packet_id}`、`{workspace}`、`{evidence_dir}` 等 argv 占位符基础上，新增 `{prompt_dir}`、`{system_prompt}`、`{user_prompt}`、`{prompt_manifest}`，外部编码 Agent 命令现在可以直接读取上一切片落盘的 Prompt Bundle，不需要自己拼 `.codentum/evidence/<worker_id>/prompt/` 路径。新增 1 条测试，证明命令可通过 `{user_prompt}` 读取 `user.md`；B 侧测试 42 条通过；目标 ruff/mypy 通过；`make verify-offline` 从 196 条增至 197 条，全绿。 |
| 08-09 | **★ B P0 切片 11：ModelGatewayRunner 最小模型调用适配器** | `packages/harness/codentum_harness/runner/model_gateway.py`。新增 `ModelGatewayRunner`，把冻结契约里的 `ModelGateway` 包装成 `WorkerRunner`：从 `.codentum/evidence/<worker_id>/prompt/` 加载并校验 Prompt Bundle digest，打开一次模型会话，调用 `ModelRequest`，把 `response.txt`、`usage.json`、`tool_calls.json`、`result.json` 写入 `model/` 证据目录。`prompt_bundle` 同步新增 `load_worker_prompt_bundle()`，防止 Runner 自己猜 prompt 文件格式。当前是 one-shot 模型调用，不含工具循环；`tool_use`、`refusal`、`max_output` 会 fail-closed 成 `WorkerFailed(model_error)`。新增 4 条测试；B 侧测试 46 条通过；目标 ruff/mypy 通过；`make verify-offline` 从 197 条增至 201 条，全绿。 |
| 08-09 | **★ AgentTeams / HiClaw 安装验证预检** | 使用 `ai-agent-development`、`agent-orchestration-multi-agent-optimize`、`architecture`、`bash-linux` 口径做本机预检。结论：Docker CLI `28.3.2`、Docker Compose `v2.38.2-desktop.1` 已可用；已启动 Docker daemon；当前只发现 `open-webui` 容器，无 `agentteams` / `hiclaw` / `higress` / `matrix` / `minio` 相关容器或卷；`:18088` 与 `:18001` 端口空闲。官方安装脚本需要 LLM Provider / API Key / 管理员配置，本机 `AGENTTEAMS_LLM_API_KEY`、`DASHSCOPE_API_KEY`、`BAILIAN_API_KEY`、`QWEN_API_KEY`、`OPENAI_API_KEY`、`ANTHROPIC_API_KEY` 均未配置，所以没有用假 Key 继续安装。下一步：拿到百炼 Key 后按官方 AgentTeams 安装脚本实跑，并验证 `docker ps | grep hiclaw-manager`、Element Web `:18088`、Higress `:18001`。 |
| 08-09 | **★ B P0 切片 12：百炼 / Anthropic Provider 具体实现** | `packages/harness/codentum_harness/model_gateway/`。新增 `OpenAICompatibleGateway`、`BailianOpenAICompatibleGateway`、`AnthropicGateway`、`TokenPricing` 与 `ModelGatewayPolicy`：百炼走 OpenAI-compatible chat completions，默认 `https://dashscope.aliyuncs.com/compatible-mode/v1`，API Key 从 `DASHSCOPE_API_KEY` / `BAILIAN_API_KEY` / `QWEN_API_KEY` / `AGENTTEAMS_LLM_API_KEY` 读取；Anthropic 走 Messages API。两侧都把 Provider 响应归一化为冻结契约里的 `ModelResponse` / `Usage` / `ToolCall`，费用按显式 token price table 计算，缺价格默认 fail-closed，避免模型调用被当成 0 成本。`runtime.py` 同步扩展 `ModelGatewayConfig` 与 `RunnerConfig.model_gateway()`，产品入口可以通过配置组装 `ModelGatewayRunner`，不把密钥值写入配置。新增 8 条 Provider / factory 测试；B 侧相关测试 14 条通过；目标 ruff/mypy 通过；`make verify-offline` 从 201 条增至 209 条，全绿。 |
| 08-09 | **★ B P0 切片 13：真实 API smoke 工具壳** | `packages/harness/codentum_harness/model_gateway/smoke.py`。新增可用 `python -m codentum_harness.model_gateway.smoke` 运行的 Provider smoke：通过 `ModelGatewayConfig` 构造百炼 / OpenAI-compatible / Anthropic 网关，打开一次会话，发送极小提示，输出不含密钥的 JSON 摘要（provider、session、model、role、stop_reason、usage、ledger_total_cny、text_preview）。默认必须显式传模型价格；除非加 `--allow-unknown-pricing`，否则缺价格直接 fail-closed，避免 smoke 把真实调用记成 0 成本。本机仍无百炼 Key，所以只完成工具与 fake-gateway 测试，真实 API smoke 等 Key 后执行。新增 4 条 smoke 测试；B 侧相关测试 15 条通过；目标 ruff/mypy 通过；`make verify-offline` 从 209 条增至 213 条，全绿。 |
| 08-09 | **★ B P0 切片 14：百炼真实 API smoke + 模型清单探测** | 使用用户提供的百炼 CSV（只本机读取，未打印/提交 Key）提取 `apiKey` 与 OpenAI-compatible endpoint，安装本地 `openai` SDK 后通过 `BailianOpenAICompatibleGateway` 实跑 `qwen-plus` smoke。结果：`stop_reason=end`，返回 `codentum-smoke-ok`，usage 为 input 45 / output 6；本次因价格表未核对，显式按 unknown pricing 跑连通性验证，`cost_cny=0` 不作为成本证据。模型列表接口可用，返回 236 个模型；与当前路由表相关候选包括 `qwen-plus` / `qwen-max` / `qwen-coder-plus` / `deepseek-v4-pro` / `deepseek-v4-flash` / `kimi-k2.6` / `kimi/kimi-k2.6` / `glm-5.2` / `ZHIPU/GLM-5.2` 等。下一步：补官方价格表或证据化价格配置，再把路由表里的模型 id 改成账号实测可调值。 |
| 08-09 | **★ B P0 切片 15：RoleSpec 模型路由实测修正** | `packages/roles/specs/` 按当前百炼业务空间**真实可调**结果修正默认模型：`coder → qwen-coder-plus-1106`，`reviewer → qwen-plus-2025-11-05`，`qa → qwen-plus-2025-07-14`。三者均通过 `BailianOpenAICompatibleGateway` 真实 smoke，均返回 `codentum-smoke-ok`，usage 均为 input 45 / output 6；仍按 unknown pricing 只作连通性证据，不作成本证据。探测中发现 `kimi/kimi-k2.6` 与 `ZHIPU/GLM-5.2` 当前业务空间未开通产品，不能写成默认路由；`qwen-max` 能返回文本但不是标准 `choices` 形态且 `usage=null`，所以 Provider parser 增加百炼 `text` 回退，但在已配置价格时遇到缺 usage 仍 fail-closed。`ModelGatewayPolicy` 的家族识别同步修正为先剥离 provider namespace（如 `siliconflow/deepseek-v3.2` 归到 `deepseek` 家族），避免后续跨供应商模型隔离判断偏掉。新增测试覆盖百炼 text 回退、缺 usage 的 priced fail-closed、namespace 家族归一化、内置 RoleSpec 的 reviewer/qa 不复用 coder 默认模型。目标 ruff/mypy 通过；`make verify-offline` 从 213 条增至 217 条，全绿。 |
| 08-09 | **★ A/B/C 接缝首次被测试覆盖，查出四处静默缺陷** | 三段各自都有测试，但**没有一个测试同时碰过两个人的代码**。新增 `tests/e2e/test_abc_integration.py` 与 `packages/desktop/data/ac-handoff.test.ts`（跨语言：A 真跑真落盘 → C 自己的 `ProjectStateSource` 加载）。查出四处**静默**缺陷：① `graph.dependency.nodes` 恒为空（`_dep_graph` 只从磁盘读、从不参与判定，纯透传）→ C 把每次全新流程都判成 `[partial-write]`；② **worker 失败仍被验收** —— 兜底只判「evidence 非空」，而那条 `sys:lock:` 是控制面自己写的簿记，等于系统自己给自己签字；③ `.codentum/` 只写了 6 个成员中的 2 个，且 `BudgetTracker` 记的账从不落盘；④ B 侧 `CommandRunner` 按 UTF-8 解码任意外部工具输出，Windows 上 `UnicodeDecodeError` 抛在**读取线程**里→线程死→`stdout=None`→下游炸出 `data must be str, not NoneType`（报错位置与真因完全无关，正是那两条老红灯的根因）。修复归属按「谁对改谁」定 |
| 08-09 | **★ P0 首次真实跑通 —— 真因不是 AgentTeams，是 runner 没配** | 修完上述四项后 worker 仍全部 `runtime_error`，一度判断是「AgentTeams 没装」，**这个判断也是错的**。直接向 runtime 要 `outcome.detail`，拿到 **`no worker runner configured`** —— `LocalWorkerRuntimeConfig.runner` 默认 `None` 就是「没有执行器」。B 的 runtime 一直是好的（建 worktree、写 manifest/checkpoints/events、渲染完整 prompt bundle 全部正常）。配一行 `RunnerConfig.command_runner(...)` 后全链路真实跑通，worker 真的建出 `src/app/feature.py`、`git status` 可见、证据 `file:runner/result.json`（非 `sys:` 前缀）。**全程没有 Docker。**顺带修掉：控制面丢弃 `outcome.detail`（`runtime_error` 一个码对应无数真因，排查被带偏整轮）· `render.py:127` 写 `budget_usd:` 却取 `limit_cny`（¥5 被告知模型是「budget_usd: 5」，差约 7 倍）· `spent=$` 显示 CNY |
| 08-09 | **★ B/Main 整合：合入队友修复并对齐 CNY 契约** | 将 `origin/main` 的 14 个团队提交合入 `feat/harness`：包含 A 控制平面 7/7 模块、C desktop v2、delivery、CNY 契约切换与 A+B 闭环记录。解决 `docs/项目进展与记忆.md` 冲突，并把 B 新增的 ModelGateway / ModelGatewayRunner / smoke / pricing / runtime 配置从 `*_usd` 字段迁到 main 的 `*_cny` 冻结接口；Prompt Bundle 里的预算字段同步改成 `budget_cny`。CNY 对齐后再次实跑百炼 smoke，`coder/reviewer/qa` 三个默认模型均返回 `codentum-smoke-ok`，usage 字段为 `cost_cny/input_tokens/output_tokens`。B 范围 pytest 62 条通过，ruff/mypy 通过；全量 `make verify-offline` 326 条通过并完成 secret-scan。额外全仓 ruff/mypy 仍暴露历史工程配置问题：contracts/scripts 里大量中文全角标点触发 RUF00x，mypy 对 `scripts/lib/console.py` 有重复模块名映射；未作为本次 merge 阻塞项处理。 |
| 08-10 | **★ 记录丢失修复 + 三件积压小事清理** | ① 发现 08-09 两次 merge（`46f1dca` / `3ea9997`）在解 `docs/项目进展与记忆.md` 冲突时**整段丢掉了 A 当天写的 85 行**——包括「P0 真实达成」状态行、四处静默缺陷排查记录、§十七 整节。已从 `ba7490f` 取回并与 B 的新内容逐节合并（保留 B 的 §十六 CNY 版本，恢复 A 的 §十七）。教训见 §十八。② 补 **ADR-0006**（成本口径 USD→CNY），采用**追记**体例：如实写明变更 `50bca30`（08-06）先于 ADR 发生，当时既未走变更窗口也未写 ADR；ADR 索引同时补上此前遗漏的 0005。③ `interfaces.py:419` 注释「只有 usd」改为 cny 并指向 ADR-0006（字段早已是 `limit_cny`/`spent_cny`/`cost_cny`，这行注释向读代码的人说反话说了四天）。④ `boundaries.yaml` 填上 B/C 的 `gitAuthor`：B=`jjc`，C=`赵心怡`（她 08-08 把 user.name 从 `zxy123` 改成 `赵心怡`，同一邮箱；**待 C 本人核对本机 `git config user.name`**）。`check_boundaries` 通过，`tests/contract` 105 条通过。提交 `b6668e9`，已推 Gitee main（快进，无历史改写） |
| 08-10 | **★ 进度全面核实（以代码与 git 为准）** | 逐文件核实后修正三处此前的判断：① **「接了真实模型」要打折** —— 管道齐但 e2e 仍走 `command_runner`，真实模型从未在控制平面内跑完一个 packet，且没有任何测试守这件事；② **「C 的交互链路未通」是记错了责任方** —— C 侧从 UI 到 Python 协议全部接完（`App.tsx:58` → `IPC_CHANNELS.engineCommand` → `PythonEngineClient` → `delivery/protocol.py`），缺的是把 `engine_proxy` 接到 `ReconcileLoop` 的那个引擎进程，**全仓库没有任何非测试代码 import `codentum_delivery`**，`engine_proxy` 唯一对话过的引擎是 `tests/_fake_engine.py`。这个缺口落在三人边界之间，`boundaries.yaml` 里谁都没认领（见待办 15）；③ **AgentTeams 的阻塞理由已不成立** —— Key 08-09 已到位。另查出两处工程卫生问题（见待办 16）。分析报告在桌面《Codentum项目完成情况全面分析.md》 |
| 08-10 | **★ A：护栏消融对照实验 + 门禁缺陷修复** | 产出 P5 要的那张「对照数据图」——做的不是进化增益曲线（P4/P5 没实现，硬做只能编），改成**护栏消融**：注入已知故障的 packet 在「护栏开/关」下跑完整 `ReconcileLoop`，数静默放行数。`scripts/experiment_guardrails.py`（零依赖，自绘 SVG）+ `docs/experiments/`。**搭实验时当场抓到门禁层缺陷**：四个内置门禁全部放行「证据只有 `sys:` 前缀」的 packet，即 08-09 在兜底分支修过、门禁分支没修的同一个洞，且门禁分支优先级更高——**配了 `gate_runner` 的系统反而比没配的更松**。判据收进 `evidence.py` 共用，回归防线实测会红。教训见 §十九。提交 `7e0b0a7` |
| 08-10 | **★ A：真实模型首次在控制平面内跑完一个 packet** | 用户提供百炼 Key，`qwen-coder-plus-1106` 走完 `pending → ready → running → review → accepted`，原始证据存 `docs/experiments/real-model-run/`（未编辑）。**当场暴露两个更深的缺陷**：① 契约里没有「要做什么」字段——模型返回的是 blocker 报告而且**模型是对的**，实发 prompt 里 `Visible Context: (none)`；之前没发现是因为 command_runner 那条 e2e 的 worker 是写死脚本、**根本不需要知道任务是什么**；② worker 自陈干不了、系统照样验收。①记为待发起 ADR-0007（触及冻结契约，不单方面改），②用 `xfail(strict=True)` 钉住。提交 `04f8654`。详见待办 21/22 |
| 08-10 | **C：任务隔离 + Codex 风格工作台**（`327c0fb`，25 文件 +1651/−272） | C 当晚提交，交付线由 0 变正。真实实现（非假状态）：附件提交时重算 SHA-256 复核原路径、联网模式确实被阻止且标为未接入、挂起任务只取权威态 blocked/pending。同时带来 `CODEX_WORKBENCH_TASK_BOOK.md`，**给 A 派 5 项、给 B 派 6 项**。A 已逐条核对，结论见待办 23–26：A.1/A.2/A.3 的前提不成立（Intake 与 pause 均无实现，`waiting` 不是控制平面状态），B.6 派到了 C 自己的 owner 路径下，`OperatorCommand` 从来就在冻结范围之外，C 那三处新保证**没有任何测试盯着** |
| 08-10 | **★ B 同步 main 最新 + 两处跨平台卫生修复** | 将 `origin/main@327c0fb` 快进合入本地 `feat/harness`，队友新增的真实模型 e2e、护栏消融实验、桌面工作台与 A/B/C 接缝测试已进入当前文件夹；本地 `feat/harness` 因此领先 `origin/feat/harness` 12 个提交。随后修掉待办 16：`ModelGatewayRunner` 写 `EvidenceRef` 时把 `relative_to(...)` 结果转为 `.as_posix()`，保证 Windows/Linux 都是 `file:model/result.json`；`test_prompt_bundle.py` 的测试读取补 `encoding="utf-8"`，不再依赖本机 locale。验证：相关 ruff 通过，B 范围 pytest 62 条通过，全仓 `make verify-offline` 352 passed / 3 skipped，secret-scan 通过。保留用户本地未提交删除 `docs/03-团队分工与协作规范.md`，不纳入本次改动。 |
| 08-10 | **★ B P1 角色编制最小闭环：11 RoleSpec + 11 prompt** | `packages/roles/specs/` 从 3 个补齐到冻结枚举里的 11 个角色：intake / architect / planner / qa / coder / helper / reviewer / integrator / manager / evolver / guardian。每个 RoleSpec 都声明 `promptRef`，新增 `packages/roles/prompts/*.md` 11 份；`codentum_roles.load_role_prompt()` 负责安全读取 promptRef，拒绝绝对路径、路径穿越和缺文件。`PromptBundle` 现在会把角色 prompt 放入 system prompt 的 `Role Prompt` 段。另修一个 A/B 接缝：当控制平面传 `tools=()` 时，`LocalWorkerRuntime._prepare()` 会用 `RoleSpec.tools` 回填内部 request，控制平面仍只调用冻结的 `spawn(req)`。测试新增到 B 范围 67 条通过；目标 ruff 通过，目标 mypy 通过；全仓 `make verify-offline` 357 passed / 3 skipped，secret-scan 通过。 |
| 08-10 | **★ B 任务意图注入最小闭环：packet-intent context** | 新增 `context_broker/intent.py`：`LocalWorkerRuntime._prepare()` 会自动把 `packet-intent` 作为 required `ContextCandidate` 注入 ContextBundle。优先读取项目内 `.codentum/packets/<packet_id>.json` 并渲染 kind / ownsPaths / readsPaths / acceptance / provenance；若文件不存在，则从 `SpawnRequest` 生成 fallback brief。这样真实模型 prompt 不再出现 `Visible Context: (none)`。这不是修改冻结 WorkPacket 契约，也不伪装成自然语言任务字段；它只是让现有契约字段以稳定、可审计的方式进入模型输入。新增测试覆盖 WorkPacket 文件路径与 fallback 路径；B 范围 69 条通过，全仓 `make verify-offline` 359 passed / 3 skipped，secret-scan 通过。 |
| 08-10 | **★ B blocker 报告 fail-closed：不把「我做不了」当交付物** | `ModelGatewayRunner` 新增确定性 blocker 检测：当模型以 `Blocker Report`、`blocked`、`cannot proceed`、`visible context does not provide...`、`insufficient context...` 或中文「可见上下文不足」等明确形式自陈阻塞时，即使 `stop_reason=end` 且没有 tool calls，也写 `model/result.json(status=failed, error=blocker_report)`，并返回 `WorkerFailed(acceptance_not_met)`。这一步不做宽泛语义验收，也不解析普通说明文字；它只兜住已实测出现的自陈失败形态，避免验收层把失败报告当成果证据。真实模型 e2e 的旧 `xfail(strict=True)` 已摘掉，改为「模型本次确实返回 blocker 时断言不得 accepted；否则跳过该内容型断言」。新增 2 条 runner 单测覆盖正例与误伤反例；B 范围 71 条通过，全仓 `make verify-offline` 361 passed / 3 skipped，secret-scan 通过。 |
| 08-10 | **★ B AgentTeams / HiClaw 本机安装验证完成** | 使用官方 `agentteams-install.sh` 非交互安装 AgentTeams v1.2.2。安装前先用用户本地百炼 CSV 脱敏读取 `apiKey` 与 OpenAI-compatible endpoint，分别验证 `qwen-plus`、`qwen-coder-plus-1106`、`qwen3.6-plus` 均可调；安装时选 `openai-compat + qwen3.6-plus`。结果：`agentteams-controller`、`agentteams-manager`、`agentteams-dashboard` 均 running；Element Web `http://127.0.0.1:18088/#/login`、Higress Console `http://localhost:18001`、Dashboard `http://localhost:13000/`、Manager Console `http://127.0.0.1:18888` 均返回 200。截图与脱敏验证记录在 `docs/experiments/agentteams-install/`；本地凭据在 `~/agentteams-manager.env` 与 `~/agentteams-install.log`，明确不入库。 |
| 08-10 | **★ B AgentTeams Worker 创建 smoke：coder Running** | 为避免只证明“登录页能打开”，继续用官方 `agt` CLI 在 `agentteams-controller` 内创建 `coder` Worker：`agt create worker --name coder --runtime copaw --model qwen3.6-plus ...`。结果：`agt get workers` 显示 `coder  Running  qwen3.6-plus  copaw`，`agt worker status --name coder -o json` 显示 `containerManaged=true`、`containerState=running`、Matrix user `@coder:matrix-local.agentteams.io:18080` 与 room id 已生成，Docker 中 `agentteams-worker-coder` 容器 running。注意：`create worker --wait-timeout 5m` 未等到更严格的 Ready hook，报 `phase=Running, state=running, message=backend=docker status=running`；因此汇报口径应是“Worker 资源/容器创建成功并 Running”，不要说已经完成 Codentum Team-mode 适配。 |
| 08-12 凌晨 | **★ A：桌面端首次真连真引擎，一夜撞出并修完七个缺陷** | 目标只有一件：让桌面端真的连上真引擎跑一次真需求。撞出七个问题，**全是「各段都对、合起来不通」，且全都只有真把应用跑起来才会现形** —— 10 张截图 / 52 桌面测试 / 429 Python 测试此前一个都没拦住。① sidecar 路径写死（C）② draftScope ≠ sourceId，界面提交从没成功过（C）③ `.codentum/` 残缺排开五条 missing（**我引入的回归**）④ 引擎不读 `CODENTUM_PROJECT_ROOT`，协议只实现一半（**我漏的**）⑤ 铺目录依赖 git 子进程（**我引入的**）⑥ 启动依赖 PYTHONPATH，「能不能跑」取决于谁开的终端（**我的**）⑦ RoleSpec 没有项目投影，界面「系统岗位 11 / 项目投影 0」（**我的**）。★ 最难查的是①⑥那类：引擎没启动 → 自己没日志；`_drain_stderr` 有意丢弃 stderr 文本；桌面端只剩一句 "A/B engine handshake failed" —— **三层无人知情**。已加 `.codentum/engine.log`。★ 过程教训：「引擎断了」我先猜了三轮才去取证，log 加完当轮就定位。教训见 §二十四 |
| 08-11 | **★ A：缺陷二的另一半补齐 —— 判据换成「工作区里多了什么」** | `_try_running_to_review` 开始看 `WorkerCompleted.touched_paths`：`kind ∈ {impl,test,fix}` 自称完成却零改动 → 落 `sys:worker-failed:...:acceptance_not_met`，停在 review。★ 数据一直都在（冻结契约里的字段，worker 一直在报），**控制平面从没看过一眼** —— 不是缺数据，是缺判定。★★ 当天就踩了第二次：真链路日志写「改动 10 个路径」照样 accepted，去看全是 `.codentum/evidence/**`（harness 自己写的），**判据被系统自己的簿记满足** —— 与「拿 sys: 当证据」同一个 bug 下沉一层；单测当时全绿因为 mock 的 touched_paths 是干净的。已加 `_worker_authored_paths()` 过滤 + 归一化反斜杠。6 条回归测试，实测摘掉判定立刻红，三个对照组保持绿。真链路验证：同一需求同一链路，修复前 accepted、修复后停 review。教训见 §二十二 |
| 08-11 | **★ A：ADR-0007 敲定 + 待办 27/30 关闭** | ADR-0007 由队长直接拍板为已接受（**未走三人同意**，已在 ADR、boundaries.yaml、索引三处如实记账，第五节「需要被反驳的地方」原文保留不删）。`ReconcileLoop.admit()` 加上，引擎不再写私有字段，重复 id 直接抛错而非静默覆盖。`scripts/lib/__init__.py` 让 `make typecheck` 复活 |
| 08-11 | **★ A：mypy 251 → 0** | `make typecheck` 复活后报出 107 文件 / 251 错误（此前是「一个文件都没检查」就退出）。全部清零，现为 `Success: no issues found`。★ 源码那 71 个**没有一个是运行时 bug**，全是类型卫生 —— 门禁死了这么久并没有真的漏进缺陷。但抓到三处「现在不是 bug、迟早是」：`_MockWorkerRuntime` 少实现协议三个方法（协议以后加方法它不会有任何反应）· `_loop: object` 靠调用顺序而非类型保证 · `RuleFn` 两处各定义一份（§十九.1 第四个样本）。两处保留运行时校验只给 mypy 记案（信任边界）。★ 批量补 `-> None` 时正则误标了 5 个有返回值的函数，**五次都是测试当场红掉才发现** —— 机械批改必须跑在有测试的地方。见 §二十三 |
| 08-11 | **★ 整合分支同步 main + B 最新 AgentTeams 证据** | 在独立 worktree `codentum-integration-engine-harness` 中把 `origin/main@60aa334` 与 `origin/feat/harness@d514715` 合入 `integration/engine-harness`。冲突只发生在进展文档与 `test_prompt_bundle.py` 的格式差异；解析时保留 A/C 的 engine、desktop 记录，也保留 B 的 AgentTeams 安装与 Worker smoke 证据。验证中发现 engine 测试历史里有 `sk-not-a-real-key-for-tests` 这种明确假 key 触发 secret-scan 历史误报；已把当前测试占位符改为 `REPLACE_ME_...`，并让顶层 secret-scan 识别 `sk-not-a-real-key...` 这种测试占位符，同时新增回归测试证明真形态密钥仍会报警。最终 `make verify-offline`：411 passed / 4 skipped，secret-scan 扫工作区 + 1076 个历史对象通过。 |
| 08-11 | **★ main 合入整合分支，准备 Team-mode 适配** | 在干净 worktree `codentum-main` 中先把本地 `main` 快进到 `origin/main@60aa334`，再用 fast-forward 合入 `origin/integration/engine-harness@52ebb49`。这一步把 A 的 engine 装配点、C 的桌面真实工作区绑定、B 的 11 角色/prompt、packet-intent、blocker fail-closed、AgentTeams 安装与 Worker smoke 证据统一进入 main。合入后将以 main 为基线继续做 AgentTeams / HiClaw Team-mode `WorkerRuntime` 适配。 |
| 08-11 | **★ B Team-mode WorkerRuntime 适配最小壳** | 新增 `TeamWorkerRuntime`：与 `LocalWorkerRuntime` 并列实现冻结的 `WorkerRuntime.spawn(req)`，不改 contracts。`spawn()` 会复用 RoleSpec / Context Broker / packet-intent / checkpoint-0 / Prompt Bundle，把执行证据写到 `.codentum/evidence/<worker_id>/`，再通过可注入的 AgentTeams client 调官方 `agt` CLI 创建并检查 Worker 资源；`runtime_ref` 采用 `agentteams://worker/<name>`。当前明确 fail-closed：在任务派发与结果回收未实现前，`settle()` 返回 `WorkerFailed(runtime_error)`，并附 `file:agentteams/status.json` 或 `file:agentteams/error.json`，避免把“Worker 资源 Running”误报成“Codentum 任务完成”。新增 4 条 Team runtime 测试；B 范围 `packages/harness/tests`：69 passed；全仓 `make verify-offline`：415 passed / 4 skipped，secret-scan 扫工作区 + git 历史通过。 |
| 08-12 | **★ B：AgentTeams status 进入 Worker 事件流** | 补齐 Team-mode 最小可展示链路：`TeamWorkerRuntime` 在创建 Worker 与 `settle()` 状态检查时，除了写 `.codentum/evidence/<worker_id>/agentteams/status.json` / `error.json`，还会把状态投影进 `events.jsonl` 的 `progress` / `finished` 事件，payload 稳定包含 `moduleId=agentteams.worker`、`moduleLabel=AgentTeams Worker`、`moduleState`、`agentteams_worker`、`phase` 与 `status_ref=file:agentteams/status.json`。这样 C 的执行中心无需读取 AgentTeams 私有目录，也能按已有 Worker 事件投影显示 Team-mode 状态。仍不夸大：任务派发与结果回收尚未实现，Running 资源仍会在 `settle()` 中 fail-closed。验证：`python -m pytest packages/harness/tests/test_team_worker_runtime.py -q` 4 passed；`python -m ruff check packages/harness/codentum_harness/worker/team.py packages/harness/tests/test_team_worker_runtime.py` 通过；`npm test -- data/state-sources.test.ts` 18 passed；`npm run typecheck` 通过。 |

**已推 Gitee**：`6e13e44`（路径锁）· `f723fb0`（赛题对齐）· `a3981b1`（ADR-0005）
· `53ba29d`（模型路由表 + 样例需求 + 状态机）· `c1b7030`（三方集成测试）
· `8866d5a`（四处静默缺陷修复）· `2ab046f`（P0 真实跑通）
· `46f1dca` / `3ea9997`（B 模型网关线两次 merge）· `b6668e9`（ADR-0006 + 注释 + gitAuthor）
· `8007a38`（找回 merge 吃掉的 85 行）· `7e0b0a7`（护栏消融 + 门禁修复）
· `04f8654`（真实模型 e2e）· `327c0fb`（**C**：任务隔离 + 工作台）· `60aa334`（**C**：桌面端真实绑定所选工作区）
· `52ebb49`（整合分支同步 main + B 最新 AgentTeams 证据）。
推前均跑过 secret-scan（工作区 + git 历史）。

⚠️ **08-09 到 08-10 之间，main 有 3 个提交一直只在本地**（两次 merge + 收尾提交），
08-10 才推上去。**本地 merge 完成 ≠ 队友看得见**；下次 merge 完当天就推。

**已推 Gitee（B / `feat/harness`）**：`96cd62a`（RoleSpec 加载、工具面派生、挂载表派生、B 侧测试与 `pyproject.toml` 测试收集配置）· Worker / Git Worktree 隔离最小壳 + Harness 准备阶段串联 + 最小证据目录（B P0 切片 3-4；提交 hash 以 `git log` 为准）· Context Broker 最小壳（B P0 切片 5；提交 hash 以 `git log` 为准）· checkpoint-0 输入快照 + `spawn(req)` 唯一入口收敛（B P0 切片 6-6b；提交 hash 以 `git log` 为准）· 本地命令 Runner 最小适配器 + Runtime 配置/工厂入口（B P0 切片 7-8；提交 hash 以 `git log` 为准）· Worker Prompt Bundle + CommandRunner Prompt 输入占位符 + ModelGatewayRunner 最小模型调用适配器 + 百炼 / Anthropic Provider 具体实现 + 真实 API smoke 工具壳 + 百炼真实 smoke 与模型清单探测（B P0 切片 9-14；提交 hash 以 `git log` 为准）· `4c5d605`（稳定模型证据路径与 prompt 编码）· `b659ca1`（11 RoleSpec + 11 prompt）· `c60afc8`（packet-intent context）· `c77ae3a`（blocker report fail-closed）· `50cb151`（AgentTeams 本机安装验证）· `d514715`（AgentTeams Worker smoke）。

---

## 十、待办

1. ✅ ~~六套方案横向选型~~ —— 已完成（08-01），Codentum 选定并完成 14 项机制整合
2. ✅ ~~仓库骨架~~ —— 已完成（08-02），`D:\Run2AGI\codentum\`
3. ✅ ~~语言与运行时选型~~ —— ⚠️ **08-03 推翻重定**：
   **Python 核心引擎 + TS 桌面端**（ADR-0003，取代 ADR-0002）。
   决定性理由是**团队三人 Python 熟、TS 生疏**，距初赛 15 天。
   ADR-0002 的技术论证本身没错，错在把「团队对两种语言成本相同」这个**从没验证过的假设**当成了已知。
   代价已明确记录：失去 `tsc` 免费门禁（用 mypy strict + `ignore-without-code` + Pydantic 补）·
   跨语言契约（两侧都生成，schema 唯一真源）· Electron+PyInstaller 打包（⚠️ 最大执行风险，P1 必须打通）
3b. ✅ **LangGraph 用在执行平面，不用在控制平面**（ADR-0004）。
   「四张图」是数据结构，LangGraph 的 Graph 是控制流图，不是一回事。
   ★ 边界判据：把 LangGraph 整个换掉，控制平面应当一行都不用改
4. ✅ ~~`scripts/gen-types`~~ —— 已完成（08-02）。`state/index.ts` 现在是真生成物，
   `pnpm gen:check` 可拦手改。附带完成 `scripts/validate-fixtures`（schema + 四项交叉检查）。
   **两个脚本都零运行时依赖，`pnpm install` 之前就能跑。**
5. ✅ ~~**第 0 周 = 契约冻结**~~ —— 已完成（08-02）。`boundaries.yaml: frozen_at = 2026-08-02`
   ⚠️ 若 B/C 还没实际过目 8 份 schema + 4 个接口签名，把 `frozen_at` 改回 `null` 走完确认再填
6. ✅ ~~`git init`~~ —— 已完成，仓库已上 Gitee，I3 与 secret-scan 的历史扫描均已生效
7. ✅ **P0 已达成（08-09 真实跑通）** —— 今天是 2026-08-10，距初赛截止 **6 天**（2026-08-16）。A 的 P0 主线是**三件**
   （`docs/03` 写死的）：状态机 + 路径锁 + 准入校验器
   - **A · 路径锁 ✅**（`6e13e44`）—— I1 的强制点
   - **A · 状态机 ✅**（`53ba29d`）—— 转换表从 RoleSpec 派生，强制 I6
   - **A · 准入校验器 ⬜** ← **下一件**
   - **A · 附带：写 C 的 secret-scan 验收测试 ⬜** —— QA-first 的制衡，`boundaries.yaml`
     注明「实现简单但后果严重」。⚠️ 讽刺的是这是我们自己设计的机制，第一次轮到自己用还没做
   - **B · Harness 准备阶段 ✅（工具面 + 挂载表 + Worker / Worktree 隔离 + SpawnRequest 准备 + 最小证据目录 + Context Broker 最小壳 + checkpoint-0 + `spawn(req)` 唯一入口）** ｜ B 真实 Runner 最小适配器 ✅ ｜ B Runtime 工厂入口 ✅ ｜ B Worker Prompt Bundle ✅ ｜ B CommandRunner Prompt 输入 ✅ ｜ B ModelGatewayRunner ✅ ｜ B 百炼 / Anthropic Provider 具体实现 ✅ ｜ B 真实 API smoke 工具壳 ✅ ｜ B 真实 API smoke 实跑 ✅ ｜ B 模型路由实测修正 ✅（Qwen 可调第一版；跨家族隔离待产品开通） ｜ C 三快照渲染看板 ⬜
   P0 判据：一个手工写的 packet 能被认领 → 执行 → 验收 → 合入，状态变更在 Git 可见
   **判据要三件合起来才成立** —— 锁只解决「认领时不撞车」，状态机解决「怎么推进」，
   还差「凭什么放行」（准入校验器）与**落盘到 `.codentum/graph.json`**
7b. ✅ **AgentTeams / HiClaw 本机安装验证完成（08-10，B）** —— 赛题**唯一的硬要求**（清单里成员 B 的事项）。
   使用官方 AgentTeams 安装脚本以非交互模式安装，Provider 走阿里云百炼业务空间的 OpenAI-compatible endpoint，默认模型 `qwen3.6-plus` 已先用极小请求验证可调。
   验收结果：`agentteams-controller` / `agentteams-manager` / `agentteams-dashboard` 三个容器 running；Element Web `:18088`、Higress Console `:18001`、Dashboard `:13000`、Manager Console `:18888` 均返回 HTTP 200；`coder` Worker 资源与 `agentteams-worker-coder` 容器均 Running。
   截图证据已落在 `docs/experiments/agentteams-install/`。本地凭据保存在 `~/agentteams-manager.env`，不得提交。
   ⚠️ 这一步证明 AgentTeams 本机可跑，且能创建 Worker 资源；**不等于** Codentum 已接入 Team-mode `WorkerRuntime`。`agt create worker` 的严格 Ready hook 超时，当前证据口径是资源/容器 Running，后者是后续适配工作。
7c. ✅ **百炼 API Key 已可用，本机 smoke 已跑通** —— `qwen-plus` 通过 OpenAI-compatible endpoint 返回 `codentum-smoke-ok`；模型列表接口返回 236 个模型；`coder/reviewer/qa` 三个默认模型已按账号实测改成可调 Qwen 固定版本，并跑过第一轮对照 smoke。
   仍待做：补价格表证据；确认是否开通 Kimi / GLM / DeepSeek 后再把 `mustDifferFrom` 从“具体模型不同”提升为“模型家族不同”
7d. ✅ **成本口径已切 CNY，ADR-0006 已于 08-10 补记** —— main 已完成 `USD → CNY` 契约切换；B provider/runner/smoke 已同步到 `cost_cny` / `grant_cny` / `spent_cny` / `limit_cny`；`interfaces.py:419` 的 usd 注释已修。
   ⚠️ ADR 是**事后追记**的：变更 `50bca30` 发生在 08-06，而 `frozen_at = 2026-08-02`，当时既没走变更窗口也没写 ADR，`check_boundaries` 照样放行。
   ★ 由此暴露一条门禁缺口，**建议落进 I3**：`type: contract` 的提交若未引用 ADR 编号应直接拒绝。目前 I3 只校验「改 contracts 的人是不是 A」，不校验「有没有 ADR」——**一个只检查一半的门禁，它的绿灯与「全部合规」长得一模一样。**
   🔴 仍待做：补百炼官方价格表证据，不能再用 unknown pricing 当成本依据。当前后果很具体——**桌面端 Cost 视图在录屏里会显示 ¥0 或空**，而「成本可归因」是本项目的卖点之一。
7e. ✅ **模型路由表 §四 的三条建议落地进度（角色层）** —— `qa ≠ coder` 与 `helper ≠ coder` 已在内置 RoleSpec 的 `mustDifferFrom` 中声明，Policy 测试覆盖 `qa/reviewer` 不复用 coder 默认模型；`mustDifferFrom` 的家族比较代码已支持 provider namespace 归一化，但默认未开启，因为当前业务空间只实测通过 Qwen 系列。后续要做的是补跨家族实测，不是再补角色声明。
8. ✅ ~~定样例需求~~ —— 已定（08-04）：**日常规划工具**（记每日任务 + 更新完成情况 +
   写日记 + 按完成情况给正向反馈）。任务/日记/鼓励语三模块**语义上本来就无关**，
   正常 Planner 也会这么切 —— 不是刻意拆出来的并行。鼓励语要读任务完成数据，
   天然带一条跨模块依赖，正好当 P1「依赖请求被拦截并给替代」的素材。
   否决了带认证的记账（认证的复杂度落在错误的地方，且破坏冷启动测试）与团队待办（要服务端）
9. 🟡 定图表库（甘特 + DAG）—— 前端唯一真正的选型。建议 dagre 布局 + 自绘 SVG。
   见桌面《Codentum技术栈总览.md》§四的四条判据
10. ⚠️ **推 Gitee 前必须先跑 secret-scan，包括 git 历史**（不只是工作区）——自己的产品自己先用
11. 补设计文档的**适用边界**：对已理解领域最强、对探索性项目最弱
12. 决定**问题定义端（Intake）是加厚还是明标为已知薄弱**
13. 统一自进化对外口径：*"解决的是让进化可度量可回滚不失控；让进化更有效仍是开放问题"*
14. 参赛材料**向 ForgePilot 的密度靠拢**（它 410 行讲清了 91 分的内容；Codentum 的 4000 行是开发指导不是参赛材料）

---

### 08-10 新增待办（按紧急度）

15. ✅ **引擎入口已落地（2026-08-11）** —— 归 A，见 **ADR-0007**（状态：提议，待 B/C 确认）。
    新增 `packages/engine/`：`SidecarGateway` → 真引擎进程 → `ReconcileLoop` → 真模型，
    全链路已用 `tests/e2e/test_engine_live_model.py` 跑通（24 秒，HTTP 200，
    `pending → running → accepted`）。原始产物在 `docs/experiments/engine-live-run/`。

    ★ **这个包不做任何判定，它只选默认值** —— 而那三个默认值比代码重要：
    ① 四个安全组件显式全开（护栏消融实验的结论直接落在这里）；
    ② 能力表按「按下去会不会真的发生事情」报，没 Key 连 `requirements` 都报 false；
    ③ `submit_requirement` 回 `accepted` 不回 `applied`（网关超时 8 秒，模型要跑几十秒）。

    ★ **需求原文经 `context_loader` 进 `ContextBundle`，缺陷一在可运行层面解决了。**
    对比 08-10 那次的 `Visible Context: (none)`，这次 prompt 里有需求正文，
    模型也从「我做不了」变成直接开始实现。**契约缺陷仍在，仍走 ADR-0008。**

    ⚠️ 仍不能夸大：`tool_calls.json` 是空的，模型把代码写在了回复正文里，
    **没有任何文件被创建** —— `ModelGatewayRunner` 是 one-shot 无工具循环。
    且**缺陷二只修掉了一半**：B 的确定性 blocker 检测（`ModelGatewayRunner`）让**显式**的「Blocker Report / 无法继续」转成 `WorkerFailed`，引擎走同一个 runner 直接继承；但**没报 blocker、只是把代码写在正文里、一个文件都没创建**的产出，仍然 `accepted`。前一半能靠关键词判定，后一半不能 —— 判据得从「模型说了什么」换成「工作区里多了什么」。

    以下是原始记录，保留以便对照 ——

15b. ~~🔴 引擎入口无人认领 —— 当场指派给一个人~~
    `delivery` 协议两端的壳都在（C 侧从 `App.tsx:58` 一路接到 `delivery/protocol.py`），
    A 的 `ReconcileLoop` 也能跑真 worker，**中间缺一个把 `engine_proxy` 接到 `ReconcileLoop` 的进程**。
    核实方式：`grep -rln "codentum_delivery" --include=*.py . | grep -v packages/delivery` → **无输出**；
    `engine_proxy` 唯一对话过的引擎是 `tests/_fake_engine.py`。
    ★ **这个缺口落在三个人的边界之间，`boundaries.yaml` 里三个人的 responsibility 都没写它。**
    不指派的话，C 会一直等 A/B 补引擎，A/B 会一直以为 C 在接线，直到录屏那天才发现。估半天到一天。

16. ✅ **两处工程卫生已修（2026-08-11，提交 `f426b28`）** ——
    `_write_result` 补 `.as_posix()`，`test_prompt_bundle.py` 三处 `read_text()`
    补 `encoding="utf-8"`；此前那处用 `os.sep` 迁就平台的权宜写法已撤销。
    本机（中文路径 + GBK）现在 `python -m pytest` 全绿。原始记录 ——

16b. ~~🟡 两处工程卫生（合计约十分钟，别再拖）~~
    - `test_prompt_bundle.py` 的 `read_text()` 补 `encoding="utf-8"` ——
      生产代码 `render.py` 全程 utf-8，测试却走本地 locale，中文路径 + GBK 机器上必红。
      **「353 全绿」目前依赖跑测试的那台机器的 locale。**
    - `runner/model_gateway.py` 的 `_write_result` 补 `.as_posix()` ——
      `relative_to` 返回 `WindowsPath`，`str()` 出来是反斜杠，
      于是 **EvidenceRef 这个契约层字符串在 Windows 和 Linux 上长得不一样**，
      而 EvidenceRef 是要被 gate 和桌面端消费的。
      ⚠️ 当前工作区里有一处未提交修改，是把测试断言改成 `os.sep` 去迁就平台——
      **那是把跨平台不一致固化进契约，不是修复。** 而且这已经是第二次踩（`2fddc05` 修过一次同类）。

17. ✅ **把 e2e 换成真实模型跑一次** —— 08-10 18:43 完成，见待办 21。
    （做法不是改 `TestRealExecution`，而是新增 `test_real_model_execution.py` 并存 ——
    command_runner 那条守「模型改了代码」，model_gateway 这条守「真模型跑完链路」，
    两条判据不同，合并会丢掉其中一条。）

18. ✅ **B：8 个 RoleSpec + 11 段 prompt 已补齐（08-10）**
    `packages/roles/specs/` 已覆盖冻结 RoleId 的 11 个角色；`packages/roles/prompts/`
    已补 11 份 prompt；`PromptBundle` 已接入 `RoleSpec.promptRef`，真实模型输入能读到角色提示词。
    仍未完成的是 `skills/` 的真实 Skill manifest 与运行时能力注册，这一块不要在汇报里说成完成。

19. ✅ **A：对照数据图已产出（08-10）** —— `scripts/experiment_guardrails.py`
    + `docs/experiments/`（JSON 原始数据 + 自绘 SVG + README）。
    做的不是「进化增益曲线」——P4/P5 没实现，硬做只能编。
    改成**护栏消融**：把注入了已知故障的 packet 分别在「护栏开 / 关」下跑完整
    `ReconcileLoop`，数有多少被静默放行。正好把中心主张「可靠性来自不变量」
    变成可测量的东西，且全程不需要模型、不花钱、离线可复现。
    结果：I1 路径锁 8/8 → 0、I5 重试上限 8/8 → 0、全局预算 8/8 → 0、
    门禁 32/32（修复前）→ 0（修复后）；并发压测 400 轮 × 5 线程，
    去掉互斥锁 315 轮出现多赢家（平均赢家 2.688），有锁 0 轮。
    ★ 「护栏关」不是人造对照组 —— 那就是本系统的默认配置（四个安全组件默认全 None）。
    实验本身由 `tests/test_experiment_guardrails.py` 守着（见待办 20 的教训）。

20. ✅ **A：门禁层的「系统自己给自己签字」已修（08-10）** —— **搭实验时抓到的**。
    四个内置门禁全部放行「证据只有 `sys:lock:<pid>`」的 packet ——
    那正是 08-09 在兜底分支修掉的缺陷，**门禁分支没修**，
    而门禁分支在 `_try_review_to_accepted` 里**优先于**兜底分支。
    于是结论是反直觉的：**配了 `gate_runner` 的系统比没配的更松，
    打开护栏这个动作反而绕过了那次修复。**
    根因是同一个判据写了两遍。已收进 `codentum_control_plane/evidence.py` 共用。
    回归防线 `test_gates.py::TestGatesRejectSysBookkeeping` 四条，已实测会红。
    ★ 三条教训见 §十九。

21. ✅ **A：真实模型已在控制平面内跑完一个 packet（08-10，首次）**
    `tests/e2e/test_real_model_execution.py`。用户提供百炼 Key 后当场跑通：
    `qwen-coder-plus-1106` 经百炼 OpenAI-compatible 调用，
    `stop_reason=end`，用量 input 290 / output 151，
    packet 走完 `pending → ready → running → review → accepted`，
    验收依据是 `file:model/result.json`（非 sys: 前缀的真实证据）。
    原始证据留在 `docs/experiments/real-model-run/`（未编辑）。
    ⚠️ 成本仍按 unknown pricing 跑，`cost_cny = 0.0` **不作为成本证据**（待办 7d）。
    ⚠️ `ModelGatewayRunner` 是 one-shot 无工具循环，这一条**不证明「模型改了代码」**——
    那条判据仍由 `TestRealExecution`（command_runner）守着。两条合起来才是完整 P0。

22. 🔴 **★ 首次真跑当场暴露的两个缺陷（08-10）**
    模型返回的不是代码，是一份 **blocker 报告**：「可见上下文里没有任何任务细节」。
    **模型是对的** —— 看实际发出去的 prompt（`docs/experiments/real-model-run/prompt-user.md`），
    `Visible Context: (none)`，全文没有一句「要做什么」。

    **缺陷一：契约里没有「要做什么」这个字段。**
    `WorkPacket` 的字段是 id / kind / state / role / ownsPaths / readsPaths /
    deps / acceptance / budget / routing / attempts / evidence / provenance ——
    没有任务描述。任务意图只能靠 `ContextBundle` 注入，而它是可选的。
    08-10 B 已先补 Harness 侧最小闭环：每次 worker spawn 都自动注入 required
    `packet-intent` context，优先从 `.codentum/packets/<packet_id>.json` 渲染现有字段，
    没有文件时用 `SpawnRequest` fallback。它能解决空上下文，但**不等于契约已有自然语言任务字段**。
    ★ 为什么之前没发现：command_runner 那条 e2e 的 worker 是写死的脚本，
    **它根本不需要知道任务是什么**。换成真模型，缺口立刻现形。
    → 这是「用假替身测出来的绿灯」的又一个样本：**替身不需要的东西，测试也测不出缺。**
    → 涉及已冻结契约（`frozen_at = 2026-08-02`），按 I3 需三人同意 + 新 ADR + 变更窗口，
      **不由一个人直接改**。记为**待发起 ADR-0007**。

    **缺陷二：worker 自陈干不了，系统照样验收。**
    `stop_reason=end` 且无 tool_calls → runner 判 completed → 验收看到一条真实证据 → accepted。
    **「我做不了这个任务」这份报告，被当成了交付物。**
    ★ 这正是 §十五 推论 2：**「可判定」不等于「判得出差别」。**
    08-09 修的是「拿控制面自己的簿记当证据」，08-10 上午修的是「门禁层同一个洞」，
    这一条更深一层：**证据是真的，内容却说的是失败。**
    B 已补一层窄口径 fail-closed：`ModelGatewayRunner` 会识别明确 blocker 标题/上下文不足报告，
    写 `error=blocker_report`，并返回 `WorkerFailed(acceptance_not_met)`；这能挡住首次真跑里出现的
    「我做不了」被当成交付物问题。
    但它**不是完整语义验收**：模型输出“看起来像完成”但其实没满足 acceptance，仍需后续评审/验收谓词设计。
    → `test_blocker_report_should_not_be_accepted` 已摘掉旧 `xfail(strict=True)`：
      真实模型本次确实返回 blocker 时断言不得 accepted；没返回 blocker 时跳过该内容型断言。
      确定性回归由 `packages/harness/tests/test_model_gateway_runner.py` 的假 gateway 单测守住。

### 08-11 新增待办（接引擎入口时当场发现的）

27. ✅ **已修（08-11）：`ReconcileLoop.admit(packet)` 已加，引擎不再写私有字段**
    重复提交同一个 id 会直接抛错而不是静默覆盖状态与证据。
    准入校验仍由调用方负责 —— 控制平面不接触 RoleSpec，把 AdmissionChecker
    塞进来会破坏那条边界。原始记录 ——

27b. ~~🟡 `ReconcileLoop` 没有公开的准入入口~~ —— 引擎现在直接写了
    `loop._packets[...]` 与 `loop._dirty`。现有调用方（测试）都是「先把
    packet 文件写进 packets/ 再 load_state」，在引擎里复刻那套会让准入校验
    与落盘之间出现一个窗口。正确的修法是给 `ReconcileLoop` 加一个公开的
    `admit(packet)`，**那是 A 的控制平面改动，不该顺手塞进接线**。约半天。

28. 🔴 **子进程会继承协议通道 —— 已在入口层堵住，但根因还在**
    `LocalWorkerRuntime.__init__` 构造 `GitWorktreeManager`，后者跑
    `git rev-parse`。这个 git 子进程继承了引擎的 fd 0，而 fd 0 正是桌面端
    发来的协议管道、主线程还在阻塞读它 —— 实测**卡死 240 秒**，
    日志里一切正常、无任何报错。
    ★ 更严重的是静的那一面：**任何读 stdin 的子进程都会把桌面端发来的命令
    从协议通道里读走** —— 命令消失，桌面端等到超时，引擎这边什么都没发生。
    不报错、不可复现、没有测试会红。
    已在 `__main__._detach_protocol_stdin()` 修（dup 协议通道，fd 0 换 devnull），
    装配从 240 秒降到 1.9 秒。
    → **但 B 那边的 `subprocess.run` 仍然没有显式 `stdin=DEVNULL`。**
    入口层这一道够用，可它依赖「所有子进程都由引擎派生」这个前提。
    建议 B 顺手补上，属纵深防御。

29. 🔴 **`pyproject.toml` 的 `testpaths` 漏了 `packages/delivery/tests`**
    —— **C 的 15 条测试从来没有进过 `make test`**。
    讽刺的是 Makefile 里早就写着正确的判断（「不写死路径，否则新增测试目录
    会静默不被覆盖，收集范围交给 testpaths 保持单一来源」），
    但**单一来源本身是手维护的**，新增包时没人记得回来改。
    已修，并由 `tests/test_testpaths_coverage.py` 三条守住（实测会红）。
    ★ 教训见 §二十。

30. ✅ **已修（08-11）：`make typecheck` 复活，并把它暴露的 251 个错误清零**

    加 `scripts/lib/__init__.py` 之后，mypy 从「一个文件都没检查」变成
    「检查 107 个文件、251 个错误」。**这 251 个已全部修完，现在是
    `Success: no issues found in 107 source files`。**

    修之前的分布：control-plane 104 · delivery 55 · scripts 49 ·
    engine 33 · harness 4。其中源码 71 / 测试 180。

    ★ **源码那 71 个里没有一个是运行时 bug** —— 全是类型卫生：
    循环变量重名（`for f in findings` 撞上前面的 `f: str`）、
    `fail()` 没标 `NoReturn` 导致下游误报 possibly-undefined、
    抽象方法用 `...` 当函数体、泛型缺参数。
    这对正确性是好消息：门禁死了这么久，**并没有真的漏进缺陷**。

    ★ 但修的过程里确实抓到了三处「不是 bug、但迟早是」的东西：

    1. **`_MockWorkerRuntime` 少实现了协议的三个方法**（`events` /
       `resume` / `adopt`）。它一直靠结构化匹配「碰巧像」`WorkerRuntime`，
       而**协议以后加方法，这个替身不会有任何反应** —— 只是悄悄地不再是
       WorkerRuntime，测试照样绿。已改为显式继承 Protocol，现在协议变了
       这里立刻红。
    2. **`ReconcileLoop._loop` 声明成 `object`**，settle 分支直接
       `self._loop.run_until_complete(...)` 没有 None 检查。当前调用顺序
       保证了 settle 之前一定 spawn 过，所以没出过事 —— 但那是**调用顺序**
       给的保证，不是类型给的。已收敛到 `_event_loop()`。
    3. **`RuleFn` 在 `rules.py` 和 `checker.py` 各定义了一份。**
       同一个签名写两处，改一处忘一处就漂移 —— 与 §十九.1 是同一个病。
       已合并为一份。

    ⚠️ 修法上有两处**保留了运行时检查**、只是给 mypy 记了案：
    `engine_proxy` 对 argv 的 `isinstance` 校验（信任边界：命令来自环境变量，
    调用方未必过类型检查）与并发压测里的 `r is not None`。
    删掉它们能让 mypy 更干净，但那是把「运行时校验」当成「类型已保证」。

    原始记录 ——

30b. ~~🟡 `make typecheck` 在本机跑不动~~ —— `scripts/lib/console.py` 报
    "Source file found twice under different module names"，mypy 直接中断，
    **后面所有文件都没被检查**。这不是新问题，但此前没人报过 ——
    因为它是「报错」不是「变红」，在终端上滚过去了。
    引擎包单独跑（配好 MYPYPATH）是干净的；控制平面另有 19 处既有告警。
    修法是给 `scripts/lib/` 加 `__init__.py` 或用 `--explicit-package-bases`。

---

> ★ **一句提醒**：08-09 → 08-10 工程线前进 2,349 行，**交付线前进 0**。
> 三个人各自都在做「正确的事」，但没有一个人在做「8.16 那天要交的东西」。
> 剩 6 天，判断一件事该不该做的唯一标准应当变成：
> **它会不会出现在那三分钟里，或者出现在那张图上。**
>
> （08-10 20:27 更新：C 当晚提交 `327c0fb`，交付线不再是 0 —— 见待办 23。）

---

### 08-10 晚间：C 提交任务书，核对结果（待办 23–26）

C 在 `327c0fb` 里带来 `packages/desktop/docs/CODEX_WORKBENCH_TASK_BOOK.md`，
给自己列 10 项、**给 A 派 5 项、给 B 派 6 项**。以下是逐条核对，不是转述。

23. 🔴 **派给 A 的 A.1 / A.2 / A.3 写成了「轻量改动」，实际是同一个缺口的三个面**

    | | C 的措辞 | 核对结果 |
    |---|---|---|
    | A.1 | 「开放 `submit_requirement` 能力，保留未知 payload 字段」 | 后半句**已经满足** —— `gateway.py:302` 是 `dict(payload)` 整体透传，不筛字段。前半句**就是待办 15**：`submit_requirement` 全仓只是 `delivery/protocol.py` 里的一行名字映射 |
    | A.2 | 「Intake 创建计划时把 `taskId` 写入运行关联信息」 | **Intake 不存在**。`packages/control-plane/` 下只有 admission / budget / evidence / gates / guardian / locks / reconcile / state_machine；`Intake` 只出现在文档、`RoleId` 字面量和桌面端投影里。这条的前提不成立 |
    | A.3 | 「**现有** `pause_at_safe_point` 产生 `waiting` 投影时补暂停原因」 | 「现有」不成立，同样只有一行名字映射，控制平面没有 pause 实现。且 **`waiting` 不是控制平面的状态** —— `PacketState` 是 pending/ready/running/blocked/review/accepted/rejected/abandoned，`waiting` 是桌面端 `domain.ts:267` 自造的投影态。要补「暂停原因」得先决定它落在哪个契约字段上 |
    | A.4 | 「权限以 RoleSpec / 路径锁 / Guardian 为上限，绝不信任 `full_access`」 | 认，本来就是这么设计的。但 `requestedAccessMode` 在 Python 侧一次都没出现 —— **目前没有任何东西会去信任它**，这条现在是空头承诺，不是已生效的约束 |
    | A.5 | 「审计 `connectivityMode`，联网需域名策略 / 凭证隔离 / 限流 / 审计」 | 认，但这是**新范围**，不是 8.16 前的活。★ 按 §十三 停止规则，应明确记为初赛后 |

    ★ **A.1/A.2/A.3 不是三件小事，是同一件大事**：引擎入口至今为零（待办 15）。
    把它写成三条「轻量配合」，会让接收方以为是三个小时的活。
    → **这加强了待办 15 的紧急度**：C 已经开始按「A 那边有引擎」来设计交付字段了。

24. 🔴 **派给 B 的 B.6 派错了人 —— 落在 C 自己的 owner 路径下**

    B.6 要求 B 把 `SidecarManager.bindProject(projectRoot)`「推入共享分支」，
    但 `packages/desktop/shell/main/python-engine/SidecarManager.ts` 按 `boundaries.yaml`
    是 **C 自己的路径**（`packages/desktop/**` owner: C）。
    该方法目前确实不存在 —— 类里只有 `start` / `handshake` / `command` / `close`；
    C 在 `index.ts:96` 做了 `bindProject?.` 能力探测所以不会崩。
    **没人能「推」一个落在 C 自己 owner 路径下的方法。** 需 C 自己实现，或说明「协作快照」指的是哪个仓。

    B.1 / B.2 / B.3 / B.5 全部压在同一个前提上：B 的 RoleSpec 与 prompt。
    现状未变 —— specs 仍是 3/11（coder/qa/reviewer），`prompts/` 仍只有 `.gitkeep`（**0 份**），
    B 自 08-09 09:48 起无提交。**给 B 派新活之前，待办 18 得先落地。**
    （B.4 关于 AgentTeams 的现状描述是准确的，无异议。）

25. 🟡 **C 说「不要求修改冻结的 contracts schema」—— 属实，但原因值得记一笔**

    `gen-check` 与 I3 都不会被触发，因为 **`OperatorCommand` 根本不在 `packages/contracts/` 里**。
    它在 `packages/desktop/shared/protocol.ts` 与 `packages/delivery/codentum_delivery/protocol.py`
    **各手写一份**，两份都在 C 的包内，且**没有 `state.py` / `state.ts` 那样的 `gen_types` 防漂移机制**。

    也就是说：**操作协议从一开始就在冻结范围之外，两侧手写、可以静默漂移。**
    ★ 这正是 §十九.1「一个概念两处实现是静默缺陷的温床」的第三个样本
    （前两个：证据判据写两遍 → 门禁比兜底更松；EvidenceRef 路径分隔符两平台不一致）。
    → 不建议在初赛前动它（改冻结范围本身要走 ADR）。**记下来，别当它不存在。**

26. 🟡 **C 那三处新保证没有任何测试盯着**

    实现是真的，不是假状态 —— 逐条看过：
    - 附件提交时重算 SHA-256 复核原路径（`requirement-draft-store.ts:479`）
    - 联网模式确实被阻止（`RequirementComposer.tsx:284` guard + `:481` disabled + `:491` 明确提示）
    - 挂起任务只取权威态 blocked/pending（`HomeView.tsx:58`）

    但按本项目的判据问一句 —— **如果它坏了，哪条测试会变红？**
    `RequirementComposer.tsx` 与 `HomeView.tsx` **没有任何测试文件 import**
    （7 个 vitest 文件逐个查过 import）。删掉 `:284` 那个 `connectivityMode === 'online'` 的 guard，
    **391 条测试没有一条会变红。**
    C 新加的 `task-library.test.ts` 只断言了 default 是 `'local'`，没有一条测「online 时提交被拒」。

    任务书第 10 条承诺「未接入时必须明确阻止联网提交」——
    现在靠的是代码在那儿，不是靠测试拦着。**这是 C 区的活，一条 vitest 用例的量。**

---

## 十一、评审已知的五处经验性未知

设计无法解决、只能跑起来才知道的：

1. **契约变更频率** —— 若过高，串行变更窗口会成为新瓶颈，并行度塌回 1
2. **小样本进化增益** —— 一个项目 20 个 packet，"同类失败 ≥2 次"的聚类可能寥寥无几
3. **最小有效任务尺度** —— 六条不变量的仪式成本对小任务可能超过收益，未定义旁路
4. **模型能力窗口** —— 大量脚手架的前提是模型上下文与自主性有限；**不变量层不依赖模型能力，是最抗时间的部分**
5. **需求定义端的薄弱** —— Intake 相对执行端薄一个数量级，而真实浪费主要来自建错东西

---

## 十二、任务文档规范（同 OpenHex）

**每完成一个任务，在 `D:\Run2AGI\HistoricTask\` 写一份文档。**
任务粒度 = 一个可交付能力（如「控制平面 P0 骨架」「Provisioning 表单」），不是单个 commit。

必含三节：

1. **问题 / 需求分析** —— 原始诉求，真正的问题在哪
2. **解决 / 实现措施** —— 怎么做的，为什么这么做
3. **经验教训** —— 踩了什么坑，下次怎么避免

文件名 `<任务名>-<YYYYMMDD>.md`。**写实情**：未验证的部分标明，不要把「代码写完」说成「已验证可用」。

---

## 十三、给这个项目的停止规则

来自设计评价报告。**这套规则不许在遇到阻力时被自己放宽**（进化的不动点同理适用于人）。

```
□ 每个方向开工前先写三条 wont_have，写不出来说明边界没想清楚
□ 每件事必须能写出"什么算做完了"，不接受"做得好"这类表述
□ 超过两周看不到可演示产出的方向，强制拆或砍，不许"再坚持一下"
□ "这个有价值"不算证据；有人用了并给了反馈才算
□ 每个方向定时间预算，超 120% 强制三选一：追加 / 缩范围 / 终止
□ 参赛材料只用产品已有的真实产出，不为演示写一次性代码
```

**已知的最大风险**（不是技术）：范围膨胀。九轮设计对话中提出的需求全是加法，零减法。
三人 + Agent 移除了"一个人做不完"这堵反馈墙，膨胀的代价将不再自动显现。
六套并行方案本身就是这个模式的一次表现——好在已经收敛为一套。

---

## 十四、六套方案横评结论与整合清单

**排名**：Codentum 102 > ForgePilot 91 > Nexus 82 > DevPlex 75 > Loom 69 > AppDevForge 60（满分 120，十二维度）

**Codentum 与 ForgePilot 是同一层级，其余四份低一档。**选 Codentum 的三条理由：唯一有可证伪中心主张 · 唯一让并行度可计算 · 异常路径写得比正常路径厚。

**⚠ 利益冲突已声明**：Codentum 由本人（Claude）参与产出，横评报告开篇已列三条自查措施，并明确指出 ForgePilot 在三个维度上更强。

### 已整合的 14 项机制

| 来源 | 机制 | 落点 |
|---|---|---|
| **ForgePilot** | Git Worktree 物理隔离（与路径锁**互补非二选一**） | `00 · 状态设计` |
| ForgePilot | Skill 五态状态机 + 五条红线 | `00 · Skill 体系`、`01` |
| ForgePilot | 检索的确定性梯度（结构化事实→决策→契约→代码索引→向量→原始日志） | `00 · 上下文工程` |
| **Nexus** | 命令粒度权限（`forbidden_commands` / `network_access`） | Skill Manifest |
| Nexus | **审计哈希链** `sha256(prev + payload)` | `00 · 审计链` |
| Nexus | **对抗性测试套件**（主动越权以验证护栏本身） | `00 · 自进化`、`01` |
| Nexus | 执行证据包五类产物（含工具调用输入输出哈希） | `00 · 输出契约` |
| Nexus | 影子回放用过去 3 个真实项目数据 | `00 · 进化流水线` |
| **DevPlex** | 等价契约切换开关（把可迁移性变成可演示动作） | `00 · MCP 面板` |
| DevPlex | Skills 面板四块划分 | `00 · 可视化` |
| **Loom** | RARE-V 循环命名（Reason/Act/Repair/Verify/Evolve） | `00 · 内部规划` |
| Loom | Skill `conflicts` 互斥字段 | Skill Manifest |
| Loom | Peer-Debug（L0.5，同角色另一实例） | `00 · 分级求助`、`01` |
| **AppDevForge** | 阿里云官方云 Skills 清单（7 个） | `01 · 云 Skills` |
| AppDevForge | AgentTeams 四道安全防线表述 | `00 · 约束对齐·三` |

### 明确否掉的一条

DevPlex 的"一级进化：参数与 Prompt 自动调优，**无需人工审核，后台静默执行**"。
**静默的能力变更 = 无法归因的退化。**效果退化是静默的，等发现时已无法定位是哪次调优造成的。

### 一条执行提醒

**参赛材料要向 ForgePilot 的密度靠拢。** 它 410 行做到 91 分，Codentum 4037 行做到 102 分——按每行信息密度算 ForgePilot 更高。Codentum 的体量是**开发指导文档**，PPT 和作品简介必须压缩。评审看密度不看篇幅。

---

## 十五、写第一块代码时踩到的：并发测试默认是空转的

**这条值得单列，因为它直接威胁「我们靠不变量而不是提示词」这个中心主张的可信度。**

路径锁写完后，先做了一次「这个测试真的能失败吗」的验证：把 `LockTable`
的互斥锁整个换成空操作，跑 **400 轮**五线程抢重叠路径 ——
**依然每轮都恰好一个赢家，一次都没红。**

原因是临界区太短，GIL 在 check-then-act 之间根本没机会切走。
也就是说：那条名义上验证 I1 的并发用例，**在锁被完全删掉的情况下照样全绿**。

把 `sys.setswitchinterval` 压到 1µs 后，无锁版本立刻暴露。已作为 autouse
fixture 固化进用例并写明理由。

### 三条推论

1. **并发测试不主动制造穿插，测的就只是「串行调用两次」。**
   写并发用例时必须配一次「把锁摘掉，它会不会红」的对照实验，
   否则那条用例的绿灯与「有没有锁」无关。

2. **这与 I2「验收可判定」是同一个问题的两面。**
   我们要求每个 packet 的验收谓词机器可判定 —— 但「可判定」不等于
   「判得出差别」。一条永远返回 true 的谓词也是机器可判定的。
   ★ 准入校验器将来要考虑：能否要求验收谓词附带一个「已知失败样本」，
   证明它在错误输入上确实为假。**没有这一步，I2 挡不住空转的验收。**

3. 顺带抓到实现里一个真 bug：`next(iter(...))` 抛的 StopIteration 会被
   Python 转成 `RuntimeError: generator raised StopIteration`，堆栈指向
   调用点，真实原因（计数器与树不一致）被完全掩盖。已改成显式抛出。
   —— 这个 bug 只有在无锁压测里才会现形，串行测试永远碰不到。

---

## 十六、✅ 成本口径已切 CNY，但价格证据仍待补

main 已完成 `USD → CNY` 契约切换，当前冻结接口以人民币为预算口径：

```
BudgetGrantRuntime.limit_cny · WorkerCompleted/Failed/Aborted.spent_cny
Usage.cost_cny · ModelGateway.open(grant_cny=...)
```

这解决了 ADR-0005 带出的核心冲突：百炼以人民币计价，不能把人民币价格硬塞进
`*_usd` 字段，也不能靠固定汇率把误差藏进账本。

本次 B/Main 整合已把 B 新增的 provider/runner/smoke/pricing/runtime 代码全部对齐到
CNY 字段；真实 smoke 仍按 unknown pricing 跑，因此只能证明模型连通性，不能证明成本准确。

**08-10 补记**：这次切换的 ADR 已补上 —— [ADR-0006](adr/0006-预算口径由-USD-改为-CNY.md)，
文内如实标注为**追记**（变更 `50bca30` 发生在 08-06，而 `frozen_at = 2026-08-02`，
当时既没走变更窗口也没写 ADR）。同时修掉了 `interfaces.py:419` 那行仍写「只有 usd」的注释。
★ 由此暴露的门禁缺口见 §十待办 7d：**I3 只校验「谁改的」，不校验「有没有 ADR」。**

**剩余任务**：补百炼官方价格表证据，并把 `TokenPricingConfig` 的 CNY 数值写成可审计配置。
🔴 当前后果很具体：**桌面端 Cost 视图在录屏里会显示 ¥0 或空。**

---

## 十七、08-09 踩到的：绿灯本身可以是假的

§十五记的是「并发测试第一版是空转的」。这次是同一个病的另外三种形态，
一天之内连着踩了三次 —— 值得把它当成一类问题记住，而不是三个孤立的 bug。

### 形态一：断言「到达成功终态」，而不检查「过程是否真的成功」

`test_pending_to_accepted_with_worker` 是我们宣称「P0 全链路打通」的旗舰测试。
它的 mock worker `evidence=()` 是空的，于是 packet 走到 accepted 时，
手上唯一那条证据是 `_try_ready_to_running` 自己写的 `sys:lock:<pid>`。

**系统自己记了一笔「我拿到锁了」，被当成了验收依据。**

这类测试有个恶劣性质：**放行逻辑越松，它越容易通过**。
换句话说，它在系统坏掉的时候绿得最稳。

> **判据**：写完一条测试，先问「如果这个功能坏了，它会不会红？」
> 答不上来的，它就不是测试，是装饰。

### 形态二：新写的测试第一次就绿

`ac-handoff.test.ts` 里有一条「找不到 Python 解释器就 return」的分支 ——
通过可能只是**没跑**。

于是临时把被测的修复退回去重跑一遍：C 真的吐出了
`[partial-write] ... missing nodes: wp-ac0001, wp-ac0002`。这才确认它是真的。

> **新测试第一次就绿，先怀疑它没跑，而不是庆祝。**
> 这和 §十五「把锁摘掉看它会不会红」是同一个动作。

### 形态三：报错位置不等于出错位置

两次都吃了这个亏：

- `CommandRunner` 里，真因是**读取线程**里的 `UnicodeDecodeError`，
  冒出来的是几十行外的 `TypeError: data must be str, not NoneType`
- worker 失败的真因是 `no worker runner configured`（一行配置没写），
  而控制面只保留了 `reason_code=runtime_error`，看上去像执行环境挂了 ——
  **排查方向被带偏了整整一轮**

> **推论**：机器可读的理由码是给**调度器**用的，人可读的详情是给**排查**用的。
> 两者都要留。丢掉详情不是精简，是把成本转嫁给下一个人。
>
> 看到「类型明显不该出现」的兜底型报错（None 出现在不该有 None 的地方），
> 要往上游找那个被吞掉的异常，而不是在报错处加个判空。

### 为什么这件事对本项目格外要紧

Codentum 的中心主张是「可靠性来自不变量，不来自提示词」，为此建了一整套确定性判定。
而这四个缺陷全是**静默**的 —— 不报错、不变红，只是安静地给出错的结果。
其中最要命的一个，让「worker 失败」和「验收通过」在系统眼里长得一模一样。

**一套以「可判定」为纲的系统，自己给出了一个不可信的判定。**

同一条批评也适用于护栏本身：`ReconcileLoop` 的
`gate_runner` / `guardian` / `budget_tracker` / `transition_table`
四个安全组件**默认全是 `None`**，默认配置下护栏是全关的。
「约定不等于约束」这句话，我们对别人说过，对自己也成立。

完整记录：`D:\Run2AGI\HistoricTask\ABC接缝打通与静默缺陷修复-20260809.md`

---



## 十八、08-10 踩到的：merge 可以静默吃掉记录

§十七 这一整节，在 08-09 的两次 merge 之后**从 main 上消失了 85 行**，
连同「P0 真实达成」的状态行、四处静默缺陷的排查记录一起。
08-10 核对进度时才发现——**发现方式是「我记得写过这个，怎么搜不到」，
而不是任何检查报了错。**

### 怎么发生的

`46f1dca`（B 把 main 整合进 `feat/harness`）解 `docs/项目进展与记忆.md` 的冲突时取了自己那侧，
`3ea9997`（A 把 `feat/harness` 合回 main）沿用了那个结果。
两次 merge 各自都是"成功"的，`git` 没有任何提示，测试全绿——
**因为文档不在任何测试的覆盖范围里。**

### 为什么这条值得单列

它和 §十七 是同一个病，只是换了个宿主：

| | §十七 | §十八 |
|---|---|---|
| 坏掉的东西 | 验收判定 | 团队记忆 |
| 坏掉时的表现 | 不报错，标成 accepted | 不报错，merge 成功 |
| 谁会发现 | 没人，除非有人去看 worker 到底改没改文件 | 没人，除非有人记得自己写过 |

**这份文件开头就写着「我是派生视图，不是真源」——
而它这次丢的恰恰是只存在于它自己身上的内容**（排查过程、判断被推翻的经过、教训）。
代码和 git 是真源，但"我们当时为什么判断错了"没有第二份副本。

### 三条推论

1. **高冲突、纯追加的文档，merge 时不要"选一侧"。**
   这份文件是按时间追加的流水账，两侧的改动几乎总是**都要保留**。
   解冲突的默认动作应该是**两侧都留**，而不是挑一个。
2. **merge 完看一眼行数。** 574 → 489 是个一眼能看出的信号，
   当时没人看。`git diff --stat` 里文档的 `-85` 就摆在那儿。
3. **本地 merge 完当天就推。** 这两次 merge 在本地躺了一天，
   期间队友看到的 main 还停在 `de826e7`——**"我合完了"和"队友看得见"是两件事。**

---

## 十九、08-10 踩到的：对照组配错了，实验照样出数

护栏消融实验（`docs/experiments/`）落地过程中的三条，都是同一类问题的变体。

### 1. 「一个概念两处实现」是静默缺陷的温床

08-09 修的是判据**错了**；这次是判据**对了但只对了一半** ——
`reconcile/loop.py` 有一份 `_is_acceptance_evidence`，
`gates/builtin.py` 里四个门禁各写各的 `if not packet.evidence`。
修好的那一半让人以为整件事已经解决。

> **修一个判据前，先 grep 这个概念还有没有第二处实现。**
> 只改一处等于制造一个「已修复」的假象 —— 比没修更糟，因为没人会再去看它。

### 2. 设计对照实验比再读一遍代码更容易发现判据问题

这个缺陷不是读代码读出来的，是**为了给实验设计注入故障**发现的。
「要构造一个应当被拦住的输入」这个动作，逼着人回答「到底什么算被拦住」——
而这正是判据本身。

### 3. ★ 对照组配错了，实验照样出漂亮的数

并发那一组的第一版对照组用的是「锁根本不存在」（申请永远成功），
跑出 400/400 轮冲突。数字很好看，但它什么都没测 ——
没有锁当然每轮 5 个赢家，**等于把结论直接写进了对照组**。
改成「保留全部冲突检测、只摘掉互斥锁」之后，才得到 315/400 这个有信息量的数。

> **看到对照组给出 100% 或 0% 的极值，先怀疑它被设计成了必然结果，
> 而不是庆祝差异显著。**

⚠️ 这和 §十五 是一对，方向相反：
那次对照组**太弱**（摘了锁但没压 `setswitchinterval`，无锁版照样全绿）；
这次对照组**太强**（摘得太多，必然全红）。两个方向都让实验失去意义。

### 4. 实验本身也要回答「如果它坏了，哪条测试会变红」

一张图只要还能画出来，没人会发现它已经不测量任何东西了 ——
柱子照样有高度，颜色照样对，只是数字不再对应事实。

`tests/test_experiment_guardrails.py` 守三件事：护栏开必须全拦住、
护栏关必须真的漏、并发对照组不许退化成「根本没有锁」。
**第三条最容易被无声破坏：把对照组换回「没有锁」，前两条依然全绿。**

完整记录：`D:\Run2AGI\HistoricTask\护栏消融对照实验与门禁静默缺陷修复-20260810.md`

---

## 二十、08-11 踩到的：接线会暴露一整类设计里看不见的缺陷

`packages/engine/` 落地那天，代码量不大。但**接的过程**当场抓出
三个缺陷 + 一条流程漏洞，四个的共同点是：**只有真的把两头接上才会现形。**
设计评审看不出来，单测看不出来，各自模块的集成测试也看不出来。

> ⚠️ 先更正一处本节初稿的错误：初稿写 `__main__.py` 是「stdio + HTTP 双模式」——
> **没有 HTTP 模式，从来没有过。** 只有 stdio JSONL 一种。
> 一份记录自己没做过的功能的进展文档，比没有进展文档更危险，
> 因为下一个人会按它去找那段不存在的代码。

### 1. ★ 子进程继承了协议通道 —— 一个「不报错的死锁」

`LocalWorkerRuntime.__init__` 构造 `GitWorktreeManager`，后者跑
`git rev-parse --show-toplevel`。这个 git 子进程**继承了引擎的 fd 0**，
而 fd 0 正是桌面端发来的协议管道，引擎主线程还在阻塞读它。实测：

```
10:31:02  已取得状态锁，开始装配
10:35:02  装配完成，开始 tick      ← 整整四分钟
10:35:24  模型返回
```

四分钟里**没有任何报错、没有任何异常日志**。查了三轮才定位，
因为「装配一个 runtime 对象」看起来根本不像是会做 I/O 的事。

**而更严重的是它安静的那一面**：任何一个会读 stdin 的子进程，都会把
桌面端发来的命令**从协议通道里读走**。命令就此消失，桌面端等到超时，
引擎这边什么都没发生 —— 不报错、不可复现、没有测试会红。

★ 修的位置比修法本身更值得记：**没有去 `worktree.py` 每个
`subprocess.run` 补 `stdin=DEVNULL`。** 那既越界（B 的路径），也只能堵住
当下这几处 —— 下一个新增的子进程调用又会漏。**fd 0 只有一个，
在进程入口一次性换掉，才是覆盖全部子进程的那个位置。**
修复后装配从 240 秒降到 1.9 秒。

> 推论：**当一个缺陷有 N 个发生点时，先找那个「只有一份」的东西。**

### 2. 库的正确默认值，是产品的错误默认值

`ReconcileLoop.run_until_stable()` 内部连跑多轮，**只在最外层保存一次**。
对测试完全正确（load → 跑完 → 断言终态）。但产品入口照抄之后，
一次真实模型调用的整段时间里，`.codentum/` 一直是初始状态，
最后一刻突然跳到终态 —— **一个卖点是「执行过程看得见」的产品，
恰好在执行过程中什么都看不见。**

这与四个安全组件默认 `None` 是同一件事的两面：
`None` 对库合理，对产品是关掉护栏；`run_until_stable` 对测试合理，
对产品是隐藏过程。

> 推论：**「默认值由谁选」必须有归属。** 在有装配点之前，
> 每个调用方各选一次，测试选一套，产品入口会选另一套，而且没人负责。
> 这是 ADR-0007 真正的内容 —— 新增一个包只是形式。

### 3. ★ 又一次：对照组配错了，实验照样出数

给「执行过程可见」写的第一版判据是：**磁盘上必须出现过 ≥2 个不同状态**。
看起来合理。实际是空转的 —— 起点 `pending` 和终点本来就是两个状态，
用退化写法（只在最后存一次）照样能看到两个。
**把产品代码改回退化写法，那一版测试全绿。**

改成**数 `save_state` 被调用了几次**，并且跑**真正的**
`_run_until_stable`（不是测试里手搓的循环 —— 手搓循环只能证明我会写循环，
证明不了产品代码是那么写的），才真的会红。

这是 §十九.3 的第二个样本，也说明那条教训**上一次并没有学会**：
知道「对照组会配错」，不等于下次配对照组时会想起来。

> 推论：**写完判据要问的不是「它现在绿吗」，是「把被测的东西改坏，
> 它会红吗」。** 前者永远能满足，后者才是判据。

### 4. 防止「测试静默不被收集」的机制，自己被静默绕过了

`Makefile` 里写着完全正确的判断：

> ★ 不写死 tests/contract。写死的话，新增的测试目录会静默地不被
>   verify-offline 覆盖 —— 而「没跑过的测试」和「跑过且通过的测试」
>   在终端上长得一模一样。收集范围交给 pyproject 的 testpaths，保持单一来源。

判断是对的，做法只做了一半：**单一来源本身仍然是手维护的。**
于是 `packages/delivery/tests` 从落地起就没进过 `testpaths` ——
**C 的 15 条测试从来没有跑过**，而每一次 `make test` 都打印「全部通过」。

> 推论：**「保持单一来源」不是终点，「单一来源本身谁来保证」才是。**
> 一个需要人记得去更新的清单，等价于没有清单 —— 只是它失败时更安静。

### 5. 这一节自己也差点丢

写到一半时，另一个会话切了分支，本节与待办 27–30 的未提交改动一起消失。
**这正是 §十八 那件事的第二次发生**，只是这次原因不是 merge 选边，
是「改完没有立刻提交」。§十八 的推论 3 已经写过「合完当天就推」——
同样，**写完当轮就提交**。

★ 五条合起来指向同一句：**接线不是体力活。**
三个模块各自都对，接起来仍然会坏，而且坏的方式不会报错。
「组件齐」到「链路通」中间的那段距离，只能靠真的接一次量出来。

---

## 二十一、08-11 合并 B 的 feat/harness：三处接缝的实测结果

`feat/engine-entry` 合入 `origin/feat/harness`。三处冲突全部**两侧都留**
（§十八 的教训这次用上了）：合并前 A 侧 1014 行、B 侧 784 行、共同祖先 779 行，
合并后 1032 行，逐项核对无内容丢失。测试文件 38 个，与 A 侧逐一对齐、一个没丢
（B 侧 32 个，差的 6 个正是引擎与 testpaths 那批）。**408 passed / 4 skipped /
412 collected**，连跑三次一致。

> ⚠️ 合并当时读到过一次 409 passed，复现不出来，也找不到对应的测试。
> 与其把那个数字写进文档，不如记下来：**「合并后测试变多了」不能作为
> 「合并成功」的证据** —— 能作为证据的是「测试文件逐个对齐、一个没丢」。
> 前者只是个数字，后者才是判据。

### 1. ✅ 「B 补完 RoleSpec 后行为自动变对」—— 预言成立

`packages/roles/specs/` 3 → **11**，`prompts/` 0 → **11**。

引擎的 `choose_acceptance_author` 之前因为 `intake` 不在已加载 RoleSpec 里，
退到了 `qa` 并在日志里留痕。合并后**无需改任何代码**，署名自动切回 `intake` ——
`test_acceptance_author_prefers_intake_once_the_rolespec_exists` 正是为这一刻写的。

> 推论：**遇到「依赖别人还没做的东西」时，退化路径要留一条会自动复原的线，
> 并且用测试把「复原条件」写下来。** 写死成 `qa` 加个 TODO 也能跑，
> 但那个 TODO 永远不会有人回来看。

### 2. ✅ 两条上下文注入是互补的，不是重复的

一度担心 B 的 `packet-intent` 与引擎的 `requirement:<packetId>` 会撞车。
实测合并后模型收到的 `Visible Context` 里两条都在，且分工清晰：

| | 来源 | 内容 |
|---|---|---|
| `packet-intent`（B） | 从**契约字段**派生 | 完成哪个 packet、什么角色、可写路径、验收谓词 |
| `requirement:<id>`（A） | 操作者提交的**自然语言原文** | 「在 workspace/ 下新建 subscriptions.py……」 |

B 那条对**任何** packet 都有（包括不是从需求提交来的），A 这条只有引擎建的
packet 才有。两条都是 `required=True`。

> 推论：两个人在同一天、互不知情地各自解决了「模型不知道要做什么」，
> **给出的答案不冲突，因为切入点不同**（一个从契约能推出什么，
> 一个从用户实际说了什么）。这不是运气 —— 是 `context_loader`
> 这个注入点本身设计得对：它允许多个来源叠加，而不是二选一。

### 3. ⚠️ 缺陷二当时只修掉了一半（另一半已于当日晚些时候补齐，见 §二十二）

B 在 `ModelGatewayRunner` 加了确定性 blocker 检测
（`Blocker Report` / `cannot proceed` / `无法继续` 等前缀 → `WorkerFailed`）。
引擎走同一个 runner，**直接继承，不需要自己再写一遍**。

**但合并后那一轮真机跑通就是另一半的样本**：模型没有报 blocker，
它把代码写在了回复正文里 —— `tool_calls` 为空、**一个文件都没被创建**、
packet 仍然 `accepted`。

> 前一半能靠关键词判定解决：**说了「我做不了」**。
> 后一半不能：**什么都没说，只是什么也没做。**
> 判据得从「模型说了什么」换成「工作区里多了什么」——
> 那是设计决定，不是补丁。

★ 记这一条是因为「缺陷二已修」是个很容易顺口说出去的结论。
`ModelGatewayRunner` 的单测确实绿了，但绿的是**能识别出显式 blocker**，
不是**能识别出没干活**。

### 4. ⚠️ 两处边界越界，都不严重，但要记

- **B 改了 `tests/e2e/**`**（A 的路径）：把缺陷二的确定性判定从真实模型 e2e
  移到 harness 单测，真实模型那条改成「只在模型本次确实返回 blocker 时才检查」。
  ★ **这个改法是对的** —— 原来那条把模型的随机行为写成了稳定判据。
  越界的是流程不是内容。
- **B 改了 `docs/**`**（A 的路径，规矩是「B/C 提 PR」）：这正是 08-09 被 merge
  吃掉 85 行的那个文件。这次没丢，因为解冲突时逐块确认了两侧都留。

> 推论：`boundaries.yaml` 保证「一条路径只有一个 owner」，但它**不阻止**
> 别人写 —— 它只是让越界这件事**事后可查**。真正的防线是 merge 时有人逐块看。

### 5. 一次完全可以避免的重复劳动

`.as_posix()` 那一行，A 和 B **在同一天各自独立修了一遍**，改法一致、
只有排版不同，合并时还撞了冲突。

根因不是谁做错了，是**待办清单里没写「谁来做」**：条目写了「要修什么」，
没写「归谁」，于是两个人都看到了、都觉得该修、都动手了。

> 推论：**待办条目必须带 owner。** 一份没有 owner 的清单，
> 要么没人做，要么两个人同时做 —— 前者更常见，后者更浪费。

### 6. ★ 一次险情：解冲突时选了 ours，差点把 B 的修复静默丢掉

合并过程中留下过一条提交：`157450f fix(merge): 应用 B 的 blocker 检测逻辑
（冲突解决时误选了 ours）`。

也就是说：`model_gateway.py` 的冲突第一次是按「取 ours」解的，
**B 那 40 行 blocker 检测就此消失** —— 而合并本身是「成功」的，
测试也全绿，因为**盯着那段逻辑的测试是 B 一起带过来的**，
取 ours 的时候它也一起没了。**缺陷和它的判据一起消失，于是没有任何东西会红。**

这与 §十八「merge 静默吃掉 85 行记录」是同一个病的第三次发作，
而且这次更危险：上次丢的是文档（只能靠人记得），这次丢的是**代码 + 它的测试**。

> 推论：**解冲突取「一侧」时，要连带检查那一侧的测试还在不在。**
> `git diff --stat` 里如果某一侧的测试文件也一起消失了，
> 那就不是解冲突，是回退。
>
> 更简单的判据：**merge 之后跑一次「被合入方的测试」**，
> 而不是只跑「合并后的全量测试」—— 全量测试是绿的，因为判据也一起没了。

★ 这次是靠人回头发现的，不是靠任何检查。

---

## 二十二、08-11 修完缺陷二的另一半：判据一直都在，只是没人看

### 修了什么

`_try_running_to_review` 现在检查 `WorkerCompleted.touched_paths`：
`kind ∈ {impl, test, fix}` 的 packet 自称完成、却**一个文件都没改**时，
落一条 `sys:worker-failed:<id>:acceptance_not_met`，停在 review，不得自动验收。

### ★ 这条缺陷为什么难抓

它是同一个病的第四层，每一层都比上一层安静：

| 层 | 表现 | 判据 | 修于 |
|---|---|---|---|
| 一 | 拿控制面自己的簿记当证据（`sys:lock:`） | 证据前缀 | 08-09（兜底分支） |
| 二 | 同一个洞在门禁分支没修，且门禁优先级更高 | 同上，收敛到 `evidence.py` | 08-10 |
| 三 | 模型明说 blocker，runner 仍判 completed | 文本前缀（B 的确定性检测） | 08-10（B） |
| 四 | **什么都没说，只是什么也没做** | **工作区里多了什么** | 08-11（本次） |

前三层都能靠「读某个字符串的前缀」解决。第四层不能 ——
证据是真的（`result.json` 确实落了盘）、模型也没说任何丧气话，
它只是把代码写在了回复正文里，工作区一个文件没变。

**判据必须从「模型说了什么」换成「工作区里多了什么」。**

### ★ 真正值得记的是：数据一直都在

`WorkerCompleted.touched_paths` 是**冻结契约里的字段**（`interfaces.py:156`），
worker 老老实实报了改过哪些文件，测试里的 `_MockWorkerRuntime` 从第一天起就在
填它。**控制平面从来没看过一眼** —— `grep touched_paths loop.py` 在本次修改前
是空的。

> 推论：**「缺判定」比「缺数据」更常见，也更难发现。**
> 缺数据会报错（字段不存在），缺判定什么都不会发生 ——
> 契约里那个字段静静躺着，看起来一切正常。
>
> 排查这类问题的一个具体动作：**把契约里的字段逐个 grep 一遍，
> 看有没有哪个只被写、从没被读。** 只写不读的字段要么是死代码，
> 要么就是一条没人执行的规则。

### 两个对照组

判据只对「必然产生文件改动」的 kind 生效（`impl` / `test` / `fix`）。
`review` / `spike` / `design` 的产出是判断和结论，本来就可能一个文件都不碰 ——
一刀切会把它们全判失败，**那不是更严格，是把判据变成噪音**。

三条测试：违规必须红 · 正常改了文件必须绿 · review 类没改文件也必须绿。
实测把判定摘掉后第一条立刻变红，两个对照组保持绿。

### ★★ 修完当天就踩了第二次：判据本身被系统自己的产物满足

改完之后跑真链路，日志写着「改动 10 个路径」，packet 照样 accepted。
去看那 10 条是什么 —— **全是 `.codentum/evidence/**`**：
harness 自己写进 worker 工作区的 prompt / response / usage / manifest。
模型的产出一个都没有。

也就是说：**「改动数 > 0」这条新判据，被系统自己的簿记满足了。**
这和「拿 `sys:` 前缀簿记当证据」是同一个 bug ——
前者污染 evidence 列表，这里污染 `touched_paths`，只是下沉了一层。

★ 而且当时**单元测试是全绿的**：mock 里的 `touched_paths` 是干净的
（`("src/app/main.py",)`），根本没有簿记文件。
**只有真的跑一次链路才看得见。**

> 推论：**新写的判据，第一件事是问「什么东西会不小心满足它」。**
> 「证据非空」被控制面自己的锁记录满足过；
> 「改动数 > 0」被 harness 自己的证据文件满足过。
> 两次都是**系统自己的产物**混进了「别人交的活」里。
> 判据的输入干不干净，和判据本身对不对，是两件事。

修法：`_worker_authored_paths()` 滤掉 `.codentum/` 前缀，并归一化反斜杠
（漏掉归一化的话，判据在 Linux 上有效、在 Windows 上失效且不报错 ——
这个坑本项目已经踩过两次）。三条测试钉着：只有簿记必须红 ·
反斜杠形式也必须红 · 簿记之外有真产出必须绿。

★ 第一版 `_worker_authored_paths` 还写错了一处：用 `lstrip("./")` 去前缀，
而 `lstrip` 剥的是**字符集合**不是前缀，`.codentum/x` 被剥成 `codentum/x`，
判定当场失效。**是刚写的那两条测试立刻抓住的** —— 先写会红的测试，
这次实实在在省了一轮。

### 真链路最终验证

同一个需求，修复前后跑同一条链路：

| | 修复前（当日上午） | 修复后 |
|---|---|---|
| 模型行为 | 把代码写在回复正文里，`tool_calls` 为空，未创建任何文件 | 同左 |
| packet 终态 | ✅ `accepted` | ⏸ `review` |
| 日志 | 「Worker 完成」 | 「Worker 自称完成但未改动任何文件（kind=impl）——「写了字」不等于「交了活」」 |

### 剩下没修的

- **`transition_table` 默认仍是 `None`** —— 打开会让 coder packet 永远停在
  review（`_try_review_to_accepted` 拿 packet 自己的 role 去问「谁能验收它」）。
  这是控制平面的建模问题，由 `test_role_transition_gap.py` 四条钉着。
- **ADR-0008（`WorkPacket` 无任务描述字段）** —— 触及冻结契约。
  运行层面已由 `context_loader` 绕过，契约缺陷仍在。
- **B 侧 `subprocess.run` 未显式 `stdin=DEVNULL`** —— 入口层已用
  `_detach_protocol_stdin()` 覆盖全部子进程，这条属纵深防御。
- **245 个 mypy 错误** —— 见待办 30。


## 附：08-11 状态快照

> ⚠️ 这一节原本编号为「二十一」，与真正的 §二十一（合并 B 的 feat/harness）
> 撞号了。状态快照不该占用教训章节的编号 —— 前者会过时，后者只追加。
> 已改为附录。

### 已完成

1. **engine 装配点合入 main**：`packages/engine/` 正式就位，delivery 协议 ↔ ReconcileLoop 已接通
2. **11 角色 + 11 提示词全部补齐**（B 的 feat/harness 已合入）
3. **AgentTeams Team-mode 运行时落地**（`packages/harness/codentum_harness/worker/team.py` + 测试）
4. **blocker fail-closed 检测生效**：模型拒绝空任务时正确返回 WorkerFailed
5. **intent 上下文注入**：packet 携带任务描述进入模型
6. **ADR-0007 敲定**（`cd29d9a`）：引擎入口归属 A
7. **对照数据图**：护栏消融实验已就位
8. **工程卫生**：EvidenceRef 跨平台、编码、testpaths 全部修复
9. ~~421 项测试全绿~~ → **08-12 凌晨已是 429 Python + 52 桌面**
10. ~~81 提交 · 28,905 行~~ → **08-12 凌晨已是 98 提交**（见 §二十四）

### 仍欠

- **录屏**（唯一出局风险，3 分钟成片）
- 三方联调 `pending → accepted` 全链路
- 参赛材料定稿

---

## 二十三、08-11：把 251 个 mypy 错误清零，以及它们证明了什么

`make typecheck` 复活之后报出 251 个错误，全部修完，现在是
`Success: no issues found in 107 source files`。

### 最值得记的结论：门禁死了这么久，并没有真的漏进缺陷

源码那 71 个错误逐条看下来，**没有一个是运行时 bug**。全是类型卫生：

| 类型 | 例子 |
|---|---|
| 循环变量重名 | `for f in findings` 撞上前面的 `f: str`（运行时正常，读的人会懵） |
| 缺 `NoReturn` | `fail()` 不标，mypy 就认为异常分支会往下走，于是在**别处**报 possibly-undefined |
| 抽象方法用 `...` 当体 | 子类忘了实现会静默返回 None，而不是报错 |
| 泛型缺参数 | `list` / `tuple` / `dict` 裸写 |

> 推论：**「门禁没跑」和「有缺陷」是两件事，别把前者当成后者。**
> 反过来也成立 —— 这次没漏进缺陷是运气好，不是因为门禁不重要。
> 一个从未执行过的检查，它的价值不体现在「这次发现了多少」，
> 体现在「下次会不会发现」。

### 但确实抓到三处「现在不是 bug、迟早是」的东西

**1. 替身少实现了协议的三个方法，而没有任何东西会说。**

`_MockWorkerRuntime` 一直靠结构化匹配「碰巧像」`WorkerRuntime`，实际少了
`events` / `resume` / `adopt`。后果不是现在会崩，而是：
**协议以后加一个方法，这个替身不会有任何反应** —— 它只是悄悄地不再是
WorkerRuntime，而依赖它的一整批测试照样绿。

已改成显式继承 Protocol。现在协议变了，这里立刻红。

> 推论：**替身要显式声明它在替谁。** 结构化匹配的便利，
> 代价是「被替身覆盖的那条链路什么时候不再被覆盖」这件事没人会告诉你。

**2. `_loop: object` —— 靠调用顺序保证，不靠类型保证。**

`settle` 分支直接 `self._loop.run_until_complete(...)`，而 `_loop` 只在
spawn 分支里创建。当前 `_active_workers` 在 `load_state` 里被清空，
所以 settle 之前一定 spawn 过 —— 跑起来没出过事。
但那是**调用顺序**给的保证。哪天有人让 settle 能从别的路径触达，
那里就是一个 AttributeError，而且只在崩溃恢复场景下偶发。

**3. `RuleFn` 在两个文件里各定义了一份。**

与 §十九.1「一个概念两处实现」是同一个病的第四个样本
（前三个：证据判据、EvidenceRef 分隔符、流编码）。已合并为一份。

### 两处保留了运行时检查，只给 mypy 记案

- `engine_proxy` 对 argv 的 `isinstance` 校验：签名写着 `Sequence[str]`，
  所以 mypy 判它冗余。但命令来自环境变量 `CODENTUM_ENGINE_COMMAND_JSON`，
  **调用方未必经过类型检查** —— 这是信任边界。
- 并发压测里的 `r is not None`。

> 删掉它们能让 mypy 更干净，但那是**把「运行时校验」当成「类型已保证」**。
> 类型检查覆盖不到进程边界，而这两处正好在边界上。

### 一个反复出现的自伤

批量给测试函数补 `-> None` 时，正则把**三个返回 WorkPacket / RoleSpec 的
辅助函数**也标成了 `-> None`；后来又把两个返回 `GateVerdict` 的内嵌门禁
函数标错。两次都是**测试当场红掉才发现的**。

> 推论：**机械批改必须跑在有测试的地方。** 同一个正则如果作用在没有测试
> 覆盖的代码上，这五处会静静地留在仓库里 —— 而它们的症状（返回值被当成
> None）要到很久以后才会显形。

---

## 二十四、08-11 夜 → 08-12：真把应用跑起来，一晚上撞出七个缺陷

今晚做的事只有一件：**让桌面端真的连上真引擎，跑一次真需求。**
代码改动不大，但撞出七个问题 —— 而它们有一个共同点：

> **七个全是「各段都对、合起来不通」，七个都只有真把应用跑起来才会现形。**
> 10 张截图、52 条桌面测试、429 条 Python 测试，此前一个都没拦住。

### 七个缺陷一览

| # | 缺陷 | 谁的 | 既有测试为何没拦住 |
|---|---|---|---|
| 1 | `resolveSidecarLaunch` 硬编码 `getAppPath()/../` | C | 截图全跑在 `CODENTUM_ENABLE_FIXTURES=1` 下，**假数据模式根本不需要 sidecar** |
| 2 | `draftScope` 与 `sourceId` 不是同一个字符串 | C | 三层各自有测试，**没人测接缝** |
| 3 | `.codentum/` 残缺，界面排开五条 `[missing]` | **A（我引入的回归）** | 没人测「刚启动、还没提交需求」那一刻 |
| 4 | 引擎不读 `CODENTUM_PROJECT_ROOT` | **A（漏实现协议的一半）** | 全仓库只有桌面端在写，**Python 侧没人读** |
| 5 | 铺目录依赖 git 子进程 | **A（我引入的）** | 把必然成功的小事绑上三个可能失败的前提 |
| 6 | 引擎启动依赖 `PYTHONPATH` | **A** | 我的测试全跑在配好环境的进程里 |
| 7 | RoleSpec 没有项目投影 | **A** | 两边各自都对，「显示 11 个」≠「接入 11 个」 |

**七个里五个是我自己引入或漏实现的。**

---

### 1. ★ 最难查的一个：诊断信息被**有意**丢掉了

用户连续三轮反馈「一打开文件夹引擎就断了」，而我这边所有测试全绿。
查到最后发现，失败原因在三层之间**全部蒸发**：

- 引擎进程根本没启动 → 它自己的日志一个字都没有
- `JsonlEngineProxy._drain_stderr` 的注释写得很清楚：
  *"Count bytes consumed without retaining potentially sensitive stderr text."*
  —— **只数字节数，不留文本**
- 桌面端最终只显示一句 `A/B engine handshake failed`

那个丢弃决定**本身是对的**（stderr 可能带凭证）。但代价是：
唯一知道真因的那一方，把话说进了一个没人听的管道。

> ★ 推论：**「不泄露」与「可诊断」是一对需要显式权衡的目标，
> 不能只做前者然后假装后者会自己成立。**
> 现在引擎把日志写进 `<project>/.codentum/engine.log` ——
> 落文件而不是回传协议：文件在用户机器上、在项目目录里，
> 查问题的人拿得到，协议通道拿不到。

★ 而更该记的是我自己的过程错误：**「引擎断了」我先猜了三轮才去拿数据。**
engine.log 是第四轮才加的，加完当轮就定位了。
正确的顺序是先让系统把原因说出来，再谈修什么。

---

### 2. ★ 启动能力不该取决于「谁开的那个终端」

引擎的启动命令是 `python -m codentum_engine`，而 `-m` 要求
`codentum_engine` 已在 `sys.path` 上 ——
也就是**要求启动 Electron 的那个终端事先 export 过 `PYTHONPATH`**。

换个终端、双击启动、从 IDE 里跑、别人 clone 下来照 README 跑，全都不成立。
而不成立时的现象正是第 1 条描述的那种「三层无人知情」。

修法不是「把环境配对」，是**让它不需要被配对**：
`_bootstrap.py` 从自己的 `__file__` 推出仓库根，把包根挂上 `sys.path`。

★ 中间有个值得记的坑：脚本模式下不能写
`from codentum_engine._bootstrap import ...` —— 那会先执行包的
`__init__.py`，而它又要导入 control-plane，
即**「为了把路径挂上去，先得路径已经挂好」**。改用 importlib 按文件加载绕开。

> 推论：**凡是「跑起来」依赖外部环境的地方，都要问一句
> 「换一台机器、换一个终端，它还成立吗」。**
> 不成立的部分，要么自举，要么在启动时明确报错 —— 而不是安静地不工作。

---

### 3. ★「显示了 11 个」不等于「接入了 11 个」

用户从界面截图上看出「系统岗位 11、项目投影 0」，并准确指出：
那 11 张岗位卡片来自桌面端自己维护的静态清单，
**不代表这 11 个角色真的被系统加载了**。

真源在 `packages/roles/specs/*.json`（B），桌面端读的是项目内的
`.codentum/roles/`（C），**两者之间原本没有任何人搬运**。
两边各自都对，没有任何测试会红。

> ★ 这是本项目「看得见的产物是真的，产生它的过程是另一回事」的又一个样本，
> 而且是**由用户从一张截图上的两个数字看出来的**。
> 「11 / 0」这个并列本身就是判据 —— 它把「系统认为有多少」和
> 「项目里实际有多少」放在了一起。
>
> 推论：**好的界面应当把「声称」与「实际」并排显示**，
> 而不是只显示一个看起来正确的数。

搬运归装配点：它是唯一同时知道「RoleSpec 从哪来」和「状态目录在哪」的地方。

---

### 4. 半个状态目录比没有状态目录更坏

引擎为了放 `engine-session.json` 先把 `.codentum/` 建了出来，
但完整形状要等第一次 `save_state()` 才铺。于是用户打开项目、
还没提交任何需求时，桌面端读到一个**存在但残缺**的目录，
界面上排开五条 `[missing]`。

**在引擎之前，新项目压根没有 `.codentum/`，界面会优雅显示「尚未初始化」——
是我把它弄脏的。**

> 推论：**引入一个中间状态时，要问它比「什么都没有」更好还是更坏。**
> 不存在是一种明确的状态，残缺不是。

---

### 5. 顺带修掉一处「文档说了、代码没做」

`_ensure_state_dir_shape` 的注释里明明白白列着形状
（graph.json · budget.json · decisions.jsonl · packets/ · evidence/ · knowledge/），
但代码只建了三个目录和 decisions.jsonl —— **graph.json 与 budget.json 从来没建过**。
而且它只被 `save_state()` 调用，后者又只在 `_dirty` 时才真写。

> 推论：**注释描述的形状要有一条测试守着**，否则它就只是一段愿望。

---

### 6. 我自己在这一夜里犯的两个过程错误

- **提交前跑了 vitest 却没等 tsc**。而 `npm run build` 的第一步就是 typecheck ——
  那次提交会让桌面端构建直接失败。50 条测试全绿不能替代类型检查。
  这是同一天内第二次踩「绿灯不代表门禁跑过」（上午是 `make typecheck`
  从来没真正执行过，这次是我自己跳过了它）。
- **遇到现象先猜，不先取证**。见第 1 条。

> 规矩：桌面端改动提交前必须 `npx tsc --noEmit && npx vitest run`，**两个都要等**。

---

★ 七条合起来，指向同一句话，而它值得写进那三分钟：

> **这套系统的价值不在于不出错，在于错误会以可判定的方式暴露。**
> 而今晚的七个缺陷恰恰说明：**判据没拿到真实输入，就等于没有判据。**
> 429 条 Python 测试、52 条桌面测试、10 张截图 —— 一个都没拦住，
> 因为它们全都跑在「已经配好的世界」里。


## 二十二、08-12 凌晨：录屏脚本就位，倒计时 5 天

### 完成

1. **录屏脚本**（`6c004a7`）：三分钟分镜 + 检查清单 + 素材清单——最大风险项已有可执行方案
2. **RoleSpec 投影**（`de7067b`）：11 份角色定义投影进 engine，研发团队页动态化
3. **AgentTeams events**（`2a5ec44`）：B 将 AgentTeams 状态投影进 worker events
4. **连夜验证**（`e88d9ec`）：真把应用跑起来，一夜撞出 7 个缺陷——已全部记录
5. **99 提交**（A 61 · B 27 · C 11）

### 仍欠

- 录屏录制（脚本就位，待执行）
- AgentTeams `docker ps` 截图
- docker/ 完整打包方案

---

## 二十五、08-12 接入现状核对（以代码为准，逐项验证）

用户提出「部分接入，没有完全接入」，并列了一份清单。逐项核对如下 ——
**判据是代码，不是文档里的说法**。

### 已接入（实测确认）

| 项 | 证据 |
|---|---|
| 11 份 RoleSpec 进入研发团队页 | 引擎 `_project_role_specs()` 写 `.codentum/roles/`，桌面 `directory-state-source.ts:262` 读 `roles/`。两侧各有测试守（`de7067b`） |
| Worker manifest / events / 状态 / 模块 / 成本 | C 的执行中心已消费 |
| 预算 · 任务 · 证据 · 路径锁 | 已接入对应页面 |
| **AgentTeams status** | **B 于 `2a5ec44` 补齐**：status 投影成 Worker `progress` 事件，payload 用 `moduleId` / `moduleLabel` / `moduleState` —— **正是桌面端 `domain.ts:258-265` 读的那几个键**。B 主动对齐了 C 的读取器，不需要桌面端读 AgentTeams 私有目录 |

★ 用户当时的判断「B 的 11 份真实 RoleSpec 没有进入研发团队页面 …… 需要 A 先提供
RoleSpec 项目投影」**完全准确**，缺口确实在 A。已补。

### 仍未接入（核实后确认）

| 项 | 归属 | 实况 |
|---|---|---|
| **Skills** | B | `packages/roles/skills/` 只有 `.gitkeep`，harness 无实现。**没有可接的东西** —— 桌面端已诚实标为「待接入」 |
| **Team-mode 任务派发 / 结果回收** | B | `TeamWorkerRuntime` 能创建并检查 AgentTeams Worker 资源，但**派发与回收未接**。★ B 的处理值得记：在接上之前 `settle()` 一律返回 `WorkerFailed(runtime_error)` 并附 `status.json` 证据，**刻意不把「Worker 资源已 Running」误报成「Codentum 任务已完成」** |
| **docker/ 打包** | C | 仅 `README.md` + 3 个 `.gitkeep`，无 Dockerfile、无编排 |
| **成本价格表** | B | `TokenPricingConfig` 仍按 unknown pricing 跑，Cost 视图会显示 0 |
| **录屏** | 三人 | 🔴 零进展，唯一出局项 |

> ★ B 那个处理方式值得单独记：**「资源已就绪」与「任务已完成」是两件事**，
> 而把前者当后者上报，正是本项目追了四层的那个病
> （§二十二：「可判定」不等于「判得出差别」）。
> B 主动选择让它 fail-closed —— 这比接上派发链路本身更重要。

### 当前门禁实况（2026-08-12 实跑）

| 检查 | 结果 |
|---|---|
| Python 测试 | **429 通过 / 4 跳过**（433 收集） |
| 桌面测试 | **53 通过** |
| mypy | **0 错误 / 108 文件** |
| tsc | 通过 |
| boundaries · secret-scan · validate-fixtures · gen-check | 通过 |
| main 提交 | **100**（A 62 · B 27 · C 11） |

---

### ★ 这次核对本身的教训

用户是**从界面截图上的「系统岗位 11 / 项目投影 0」两个数字**发现问题的，
而当时三层代码各自都对、没有任何测试会红。

> 推论：**当一个界面同时显示「系统认为有多少」和「项目里实际有多少」时，
> 差异是一眼可见的。** 如果它只显示「11 个岗位」，这个缺口能一直藏到演示当天。
>
> 这条对录屏也成立：**演示时把「声称」与「实际」并排放，比只放成功画面更有说服力。**
